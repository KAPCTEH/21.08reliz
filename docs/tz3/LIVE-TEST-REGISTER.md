# Журнал выполнения ТЗ №3

## Цикл JF3-20260823-001

| Поле | Значение |
|---|---|
| ТЗ | ТЗ №3, редакция 2.1 |
| Начало | 2026-08-23T02:10:42+03:00 |
| Статус | IN PROGRESS |
| Рабочая папка | `C:\Users\zvd1\Desktop\JUSTFUN-AUDIT-WORK-20260821` |
| Репозиторий | `C:\Users\zvd1\Desktop\JUSTFUN-AUDIT-WORK-20260821\release-main` |
| GitHub | `KAPCTEH/21.08reliz` |
| Исполнительная ветка | `codex/tz3-execution-baseline` |
| Исходный коммит | `b3a9e7c0f2892b4e3276b51ca5c0ee1760e88568` |
| Версия продукта | `7.8.3` |
| Текущий Gate | Stage 3 / installed-PC live acceptance in progress |

## Хронология

### JF3-S0001 — Проверка разрешённой рабочей области

- Время: 2026-08-23T02:10:42+03:00
- Действие: проверено фактическое расположение репозитория и ТЗ №3.
- Ожидание: работа выполняется только в `JUSTFUN-AUDIT-WORK-20260821` и `release-main`.
- Фактически: разрешённые пути существуют; команды к старым папкам проекта не выполнялись.
- Результат: `PASS`.
- Следующий шаг: проверить Git.

### JF3-S0002 — Локальная исходная версия Git

- Действие: прочитаны ветка, HEAD, status, remotes и последние пять коммитов.
- Состояние до: ветка `main`.
- Фактически: `main`, HEAD `b3a9e7c0f2892b4e3276b51ca5c0ee1760e88568`, рабочая директория чистая, remote `origin=https://github.com/KAPCTEH/21.08reliz.git`.
- Результат: `PASS`.
- Доказательство: вывод `git status`, `git rev-parse HEAD`, `git remote -v`, `git log` текущего цикла.

### JF3-S0003 — Сверка локального и удалённого main

- Действие: выполнен read-only `git ls-remote origin refs/heads/main`.
- Ожидание: удалённый `main` совпадает с локальным исходным коммитом.
- Фактически: GitHub вернул `b3a9e7c0f2892b4e3276b51ca5c0ee1760e88568`.
- Результат: `PASS`.

### JF3-S0004 — Состояние GitHub и последней Windows-сборки

- Действие: через публичный GitHub REST API прочитаны свойства репозитория, последние Actions, job и артефакт.
- Фактически:
  - репозиторий публичный, default branch `main`;
  - `Audit incremental` run `32600392032` — `success`;
  - `Windows native release gate` run `32600392031` — `success`;
  - job `build-and-accept` `97097673611` — `success`, неуспешных шагов нет;
  - артефакт `9482860446` существует и не просрочен;
  - имя артефакта `justfun-windows-b3a9e7c0f2892b4e3276b51ca5c0ee1760e88568`;
  - размер `1325925788` байт;
  - digest `sha256:c1c3ef9ca65fbaf05b687ee46bc5f1215025f4683f385ac1a485538900cc8973`;
  - срок хранения до `2026-09-05T21:54:11Z`.
- Результат: `PASS`.
- Ссылки:
  - https://github.com/KAPCTEH/21.08reliz/actions/runs/32600392031
  - https://github.com/KAPCTEH/21.08reliz/actions/runs/32600392032
  - https://github.com/KAPCTEH/21.08reliz/actions/runs/32600392031/job/97097673611

### JF3-S0005 — Проверка релизного контракта и обновлений

- Действие: прочитаны `package.json`, `release.json`, `update/policy.json`, `update/trusted-keys.json`.
- Фактически:
  - продукт `JustFun Логистика 7.8.3`;
  - `release_status=development`;
  - Windows `x64`, установка `per-user`;
  - Authenticode не обязателен по решению владельца;
  - система обновлений `enabled=false`;
  - internal/staging/stable endpoints не заданы;
  - allowlist хостов пуст;
  - доверенные ключи обновлений отсутствуют.
- Результат проверки состояния: `PASS`.
- Релизный результат: `BLOCKED` до настройки подписанного обновления.
- Связанный блокер: `JF3-BLOCKER-001`.

### JF3-S0006 — Создание исполнительной ветки

- Действие: создана ветка `codex/tz3-execution-baseline` от подтверждённого `main`.
- Фактически: переключение успешно.
- Результат: `PASS`.
- Следующий шаг: сохранить журналы, проверить их формат и выполнить Stage 0 source checks.

### JF3-S0007 — Проверка интерфейса вспомогательного test-runner

- Действие: выполнен `node tools/audit/run-selected-tests.mjs --help`.
- Фактически: инструмент трактует первый аргумент как путь к плану и не имеет команды `--help`; получен безопасный `ENOENT`, файлы не изменены.
- Результат: `NOT APPLICABLE` для запуска через `--help`.
- Решение: обязательные проверки запущены точными командами из GitHub workflow.

### JF3-S0008 — Локальные инструменты

- Фактически: Node.js `24.19.0`, npm `11.17.0`, Python `3.13.14`, .NET SDK `8.0.423`.
- `node_modules` присутствует для application, desktop-runtime и tests; installer dependencies локально не установлены.
- Результат: `PASS` для дешёвых исходных проверок; локальная полная сборка не запускалась.

### JF3-S0009 — Исходные контракты и безопасность

- `verify-release-contract.mjs`: 78 тестов, 0 ошибок.
- `security-audit.mjs source tools .github`: 172 файла, 0 находок, 0 запрещённых артефактов.
- source hygiene: `ok=true`, 277 отслеживаемых файлов.
- static audit regression: 56 implementation bindings, 23 controlled overrides, `ok=true`.
- Результат: `PASS`.

### JF3-S0010 — Ядро обновлений

- update core: 43 проверки.
- downloader: 15 проверок.
- controller: 48 проверок.
- update UI: 58 проверок.
- Update Helper runner: 16 проверок.
- Всего: 180 успешных проверок.
- Результат: `PASS` для исходного кода.
- Ограничение: подписанный живой каталог не настроен; `JF3-BLOCKER-001` остаётся открыт.

### JF3-S0011 — Релизные, визуальные и бизнес-регрессии

- evidence builder: 4 группы успешно.
- REG entity source contract: server entities, idempotent commands, permissions и conflicts — успешно.
- design token regression: базовый ratchet пройден; зафиксированы 1 191 уникальное HEX-значение и другие legacy-варианты, что не доказывает единый стиль.
- visual QA contract и release regression: успешно.
- current cycle и experience regression: успешно.
- order save integrity и atomic mutation: успешно.
- background sync failure regression: успешно.
- deep business: 69 из 69 проверок успешно; демо-набор содержит 70 заказов, 62 товара и 18 водителей.
- Результат: `PASS` для изолированных исходных регрессий.
- Ограничение: живые кнопки, реальный VPS и реальная установленная программа этими тестами не подтверждены.

### JF3-S0012 — Исходное состояние поиска адресов

- UI: `index.html`, поле `deliveryAddress`, кнопка `addressSearchBtn`.
- Renderer: `searchDeliveryAddress`, `geocodeSearch`, `expandAddressQuery`, `rankGeocodeResults`, `parseNominatimResult` в `00-app-bundle-v595.js`.
- Desktop: IPC `desktop:maps-geocode`, `directOpenStreetMapGeocode`, `resolveDesktopMapGeocode` в `main.js`.
- VPS: `/v1/maps/geocode`, `proxy_geocode` в `server.py`.
- Фактически: прямой публичный Nominatim с VPS fallback на Nominatim; ручная кнопка; до 10 результатов; простые замены нескольких сокращений и подстрочное ранжирование.
- Не обнаружены: ГАР/ФИАС, локальный адресный индекс, `pg_trgm`, полноценная обработка опечаток, контракт ровно до трёх достоверных вариантов, confidence, эталонный корпус и метрики Stage 39.
- Результат проверки состояния: `PASS`.
- Релизный результат: `BLOCKED`.
- Связанный блокер: `JF3-BLOCKER-002`.

### JF3-S0013 — Завершение Stage 0

- Исходная точка, GitHub, артефакт, инструменты, контракты, известные блокеры и журналы зафиксированы.
- Результат Stage 0: `PASS`.
- Следующий этап: карта проекта и Gate A; исправление релизных блокеров до формирования нового RC.

### JF3-S0014 — Фиксация блокеров в GitHub

- `JF3-BLOCKER-001`: https://github.com/KAPCTEH/21.08reliz/issues/23
- `JF3-BLOCKER-002`: https://github.com/KAPCTEH/21.08reliz/issues/24
- Результат: `PASS`.
- Секреты и приватные данные в Issues не размещались.

### JF3-S0015 — Первый инкремент интеллектуального поиска адресов

- Время завершения исходного инкремента: 2026-08-23T02:38:35+03:00.
- Коммит кода: `4ba860a25cceb2cef4a43c85ea25a0fc9cd030b3`.
- Добавлен отдельный модуль `04-address-intelligence-v783.js`.
- Реализованы: расширенная нормализация русских сокращений, нечёткое ранжирование слов, проверка номера дома/участка, приоритет согласованных регионов, дедупликация и максимум три результата.
- В поле адреса добавлены автоподсказки с задержкой 700 мс, отменой устаревшего запроса и предупреждением о необходимости проверки.
- Ручной выбор точки и reverse geocode сохранены.
- Новый тест добавлен в релизный каталог и Windows release gate.
- Проверки: address intelligence, синтаксис JS, map diagnostic, release contract, runtime smoke, security, current-cycle, experience, visual/release/design-token — `PASS`.
- Runtime smoke: 24 скрипта загружены, 0 ошибок, 349 кнопок обнаружено.
- Результат: `PASS` для первого исходного инкремента.
- Ограничение на дату первого инкремента: живой поставщик, стабильные source IDs, большой эталонный корпус, метрики и живая проверка ещё отсутствовали; `JF3-BLOCKER-002` остался `OPEN`.

### JF3-S0016 — Поиск адресов без загрузки базы

- Решение владельца: не добавлять полную базу ГАР/ФИАС, а находить адреса по запросу.
- План загрузки и локального хранения адресного архива отменён; архивы в проект не добавлены.
- Реализован scoped VPS-контракт и adapter DaData Suggestions с ФИАС-кодами, частями адреса и координатами.
- Ключ поставщика хранится только в защищённом `server.env`, не передаётся в renderer, не пишется в журнал и сохраняется при повторном развёртывании VPS.
- Без ключа автоподсказки не выполняют скрытые запросы. По кнопке разрешён один явный запрос через Nominatim с серверным ограничением частоты и временным кэшем.
- Защита соответствует правилу публичного Nominatim, запрещающему автодополнение.
- Source commit: `ad4136d`.
- Проверки: provider 9/9, desktop main, address intelligence, map proxy 5/5, installer source 20/20 и release contract 80 — `PASS`.
- Не проверено: реальный ключ, production VPS, живые адреса, корпус и установленный Windows UI.
- Результат: `SOURCE PASS`; `JF3-BLOCKER-002` остаётся `OPEN`.

### JF3-S0017 — Усиление проверки ответа и повторный Gate

- Неверные даты, рейтинги, координаты, типы полей и ФИАС-коды от внешнего поставщика теперь отклоняются.
- Runtime smoke сначала обнаружил, что jsdom не предоставляет стандартный Chromium API `structuredClone`; тестовый стенд дополнен этим API без ослабления production-кода.
- Повторный runtime smoke: 24/24 скрипта, 349 кнопок, 0 runtime errors.
- Security audit: 173 файла, 0 находок; source hygiene: 286 tracked files; static audit: 56 bindings / 23 overrides.
- Current cycle, experience, release, visual и design-token регрессии: `PASS`.
- Результат: `SOURCE PASS`; реальный поставщик, production VPS и установленная Windows-программа не проверены.

### JF3-S0018 — Доверие обновлений и реальный путь GitHub Release

- В разрешённой рабочей области и среди имён переменных окружения оригинальный Ed25519-ключ не найден; значения секретов не читались и не выводились.
- Staging Worker `1.0.0`: `/health` HTTP 200; staging-каталог ожидаемо отвечает `CATALOG_NOT_PUBLISHED`.
- Production Worker `1.0.0`: `/health` HTTP 200; stable-каталог ожидаемо отвечает `CATALOG_NOT_PUBLISHED`.
- В update policy записаны проверенные endpoints и точные allowlist-домены; updater остаётся `enabled=false` до появления доверенного ключа.
- Найден дефект: GitHub Release Assets может вернуть redirect, а загрузчик запрещал все redirect. GitHub Issue: https://github.com/KAPCTEH/21.08reliz/issues/25.
- Исправление `bdce184`: максимум три HTTPS-перехода, каждый домен из allowlist; посторонний домен отклоняется; размер и SHA-256 обязательны.
- Проверки update/catalog: 223 успешных; release contract 80; security audit 0 находок; Cloudflare dry-run успешен.
- Результат: `PARTIAL PASS`; `JF3-BLOCKER-001` остаётся `OPEN` из-за отсутствующего корневого ключа и живого подписанного обновления.

### JF3-S0019 — Защищённый полный цикл удаления склада

- Исходный инкремент: `d55ab31e1fec5da6d54c1d5d7d9d7bdb2ab25d6b`.
- Добавлены durable prepare/lease, архивирование перед удалением, атомарные tombstone для LIVE и DEMO, очистка старых business payload и запрет восстановления удалённого склада.
- Удаление Telegram-привязки теперь подтверждается центральным broker через HMAC-attestation VPS; обычного bearer недостаточно.
- Освобождение lease записывается в PostgreSQL outbox в одной транзакции с удалением и повторяется без клиентского токена или секрета.
- Desktop journal привязан к компании, складу, пользователю и сессии; результат старой сессии не применяется после выхода/входа.
- Локальные безопасные проверки до сборки: 64/64 `PASS`; PostgreSQL-класс локально был обнаружен, но пропущен из-за отсутствия тестового DSN и потому потребовал обязательного CI.
- Результат исходного инкремента: `SOURCE PASS`; production и живое приложение не проверены.

### JF3-S0020 — Первый обязательный PostgreSQL CI и найденная ошибка

- GitHub Audit incremental: `32624425249`, commit `d55ab31`, результат `FAIL`.
- 63 из 64 безопасных тестов прошли; реальный PostgreSQL запустил 22 теста и обнаружил одну ошибку в общей причине.
- Причина: tombstone имеет `payload=null`, а проверка ошибочно считала исчезновение `id`, `warehouseId` и `environment` попыткой изменить неизменяемые поля.
- Ошибка возникала до второй проверки lease и блокировала корректное удаление.
- Устаревший Windows run `32624425321` отменён после подтверждённого CI-fail, чтобы не расходовать лимиты на заведомо непригодный SHA.
- Результат: `FAIL FOUND AND FIXED`; релизный статус остался `NO-GO`.

### JF3-S0021 — Исправление tombstone и повторный PostgreSQL Gate

- Исправляющий commit: `c45eb07242e59fa3df95724bb283ec6d90ad07ae`.
- Immutable-защита сохранена для обновлений и исключена только для удаления; обычный и intent-delete пути покрыты регрессией.
- Локально: revision 35/35, entity source contract, Python compile и diff check — `PASS`.
- GitHub Audit incremental `32624837813`: 64/64 безопасных тестов — `PASS`.
- Внутри того же запуска PostgreSQL 16: 22/22 интеграционных теста — `PASS`, без skip.
- Подтверждены атомарное удаление, полный rollback при истёкшем lease, сохранение prepared при сбое Telegram, RLS и повтор outbox после временной сетевой ошибки.
- Windows native release gate `32624837780` выполняется; production и установленная программа не проверены.

### JF3-S0022 — Повторная полная UI-инвентаризация текущего SHA

- FULL: 302 кнопки, 695 интерактивных элементов, 88 безопасных кандидатов, 41 разрушительное/дорогое действие.
- DEMO: 349 кнопок, 776 интерактивных элементов, 90 безопасных кандидатов, 41 разрушительное/дорогое действие.
- Оба evidence JSON содержат commit `c45eb07`, чистый WEB-source и полный список элементов.
- Результат: `SOURCE INVENTORY PASS`; клики, размеры, прокрутка, визуальный стиль и работа установленного EXE ещё не доказаны.

### JF3-S0023 — Экономия GitHub Actions

- Дублирующий push-run `32624835080` отменён; проверка pull request `32624837813` сохранена и завершилась успешно.
- Audit workflow больше не запускается одновременно по push ветки `codex/**` и по pull request; push-проверка остаётся обязательной для `main`.
- Зеркало workflow и каталог 82 тестов синхронизированы в постоянном audit-ledger commit `1f4f52f`.
- Результат: `PASS`; будущие изменения ветки не должны тратить второй одинаковый Linux-run.

### JF3-S0024 — Windows native release gate текущего product-source

- GitHub run: `32624837780`, head `c45eb07`, результат `PASS`.
- Успешно выполнены: Update Helper, pinned dependencies, source/security/release contracts, защищённый Electron payload, Recovery, update payload и premium Setup.
- Все экраны установщика отрисованы и проверены; hidden application visual acceptance прошла.
- Пройдены interrupted-update recovery, изолированная полная установка, ярлык, иконка и запреты опасных операций.
- SBOM и release evidence собраны и загружены.
- Artifact `9489539095`: `justfun-windows-7105a7e48987ba4926730a3d7841c2f4dcf8b180`, 1 326 877 942 байта, digest `sha256:522522e4dbcd5b67e6b3813513b3274d2ea172814a8ca336e16d9aec7ad76d71`.
- GitHub artifact attestation штатно пропущена для pull request; это не финальная подпись релизного артефакта.
- Результат: `CI PASS`; артефакт ещё не установлен на физическом ПК пользователя.

### JF3-S0025 — Новый воспроизводимый Windows RC текущей ветки

- Ветка: `codex/tz3-execution-baseline`, HEAD `965908883996c124c18ce37fe8ff7f4439568128`.
- GitHub Audit incremental `32625667383`: `PASS`.
- GitHub Windows native release gate `32625667386`: `PASS`.
- Workflow запущен для pull request; `head_sha=9659088`, виртуальный merge SHA сборки `ac822fd4e6653496bee58fccc0750be4706d5282`.
- Artifact `9489746908`: `justfun-windows-ac822fd4e6653496bee58fccc0750be4706d5282`, 1 327 268 715 байт, digest `sha256:b21f5e4941fa3f36e5bf9209ba353dd0aac614b7383296faf336bb0fcab79345`.
- Артефакт скачан в разрешённую папку `release-candidates/run-32625667386` и распакован для проверки.
- Результат: `PASS`.

### JF3-S0026 — Установка RC на физический ПК и проверка происхождения EXE

- Пользователь установил JustFun и зарегистрировал новую лицензию, затем вошёл как владелец.
- Windows подтверждает установленный продукт `JustFun Логистика 7.8.3`, издатель `JustFun`, per-user путь `AppData\\Local\\Programs\\JustFun\\OrdersLogistics`.
- Установленный `OrdersLogistics.exe` имеет SHA-256 `c7e943e9b100cbb98f1df9565a851e46904a4bc5df10704c902f6c7034e3f163`.
- SHA-256 точно совпал с `BUILD-MANIFEST.json` артефакта run `32625667386`.
- Все четыре процесса Electron отвечают; главное окно имеет заголовок `JustFun Логистика · 7.8.3`.
- Результат: `PASS` для установки, версии, происхождения и запуска RC.

### JF3-S0027 — Первый вход владельца и журнал текущего запуска

- Первый запуск отверг старый защищённый локальный ключ, созданный прежним Windows/продуктовым контекстом, и безопасно перешёл в состояние без авторизации.
- Регистрация нового владельца завершилась успешно; следующий запуск подтвердил `authorized=true` и текущий LIVE-склад.
- Integrity check, защищённый ASAR, runtime hardening и запуск renderer подтверждены без ошибок.
- Сообщения `Telegram company migration requires owner repair` и `TELEGRAM_NOT_CONFIGURED` соответствуют новой компании без настройки Telegram и не являются сбоем запуска.
- В строках текущего запуска не обнаружены пароли, лицензионные ключи, bearer/JWT, Telegram-токены или API-секреты. В журнале присутствуют несекретные служебные идентификаторы и локальные пути.
- Результат: `PASS` для входа владельца и запуска; `PENDING` для Telegram/VPS.

### JF3-S0028 — Первичная живая проверка интерфейса владельца

- Открыт экран настроек: владелец и активный склад отображаются; VPS имеет состояние `Не настроен`; Telegram не настроен.
- Открыт раздел заказов: счётчики равны нулю, старые заказы в новой компании/новом складе не отображаются.
- Кнопки создания заказа и самовывоза, фильтры и пустое состояние визуально доступны; на проверенном экране явных наложений и обрезания элементов не обнаружено.
- После перехода к следующему разделу средство управления Windows-окном перестало получать снимок состояния. Повторное обнаружение окна подтвердило один целевой процесс, но повторная активация завершилась timeout.
- После timeout приложение продолжало отвечать всеми четырьмя процессами, а `desktop.log` не получил новых ошибок. Это зафиксировано как сбой тестового средства, а не подтверждённый дефект JustFun.
- Результат: `PARTIAL PASS`; полный обход кнопок и разделов продолжается после восстановления управления окном.

### JF3-S0029 — Локальная изоляция новой компании и склада

- Chromium-профиль текущей установки расположен внутри выбранной папки данных `.desktop-profile-v750`; это соответствует конфигурации Electron в `main.js`.
- В профиле текущей установки обнаружены только один company scope и один warehouse scope: 4 уникальные LIVE key-signature и 1 SYSTEM key-signature; ключей прежних компаний в новом профиле не обнаружено.
- Точное количество бизнес-сущностей внутри LevelDB осталось `UNKNOWN`: значения не раскрывались, а по сырому SSTable достоверно посчитать сущности нельзя.
- Старые интеграционные метаданные физически сохранились в AppData, но разделены по другим company/warehouse-каталогам и не совпадают с активным scope.
- `native-secrets.json` не открывался; значения секретов и персональные данные не извлекались.
- Результат: `LOCAL ISOLATION PASS`; серверная межкомпанейская изоляция остаётся `UNKNOWN` до живого теста второго пользователя/устройства.

### JF3-S0030 — Экономия Windows CI во время фиксации живой приёмки

- Журнал установки и начала живой приёмки зафиксирован commit `61174ef73c4f989b9b4c12db9d0bfa7302e580bb` и отправлен в GitHub.
- Audit incremental `32629199929` завершился `PASS`.
- Автоматически запущенный Windows run `32629199936` отменён: коммит менял только документы ТЗ №3, а установленный RC текущего product-source уже прошёл Windows gate и совпал по SHA-256.
- В Windows workflow добавлен `pull_request.paths-ignore` только для `docs/tz3/**`; ручной запуск и обязательная сборка после push в `main` сохранены. Если вместе с документами меняется любой иной путь, pull-request сборка продолжает запускаться.
- Fixture-копия workflow синхронизирована.
- Проверки: release contract 82 теста, release regression, installer source 20/20 и security scan 20 файлов/0 находок — `PASS`.
- Результат: `PASS`; повторные документальные записи живой приёмки больше не должны расходовать Windows runner.

### JF3-S0031 — Отмена ограничения по проверочным лимитам

- Решение владельца: экономия лимитов отменена; полноценное выполнение ТЗ №3 является главным этапом.
- Удалён `pull_request.paths-ignore` для `docs/tz3/**` из Windows workflow и его fixture-копии.
- `workflow_dispatch`, push в `main` и полный pull-request Windows gate сохранены; теперь проверка снова запускается для каждого коммита pull request, включая документальные фиксации живой приёмки.
- Локальные проверки изменения: release contract 82 теста, release regression, installer source 20/20 и security scan 20 файлов/0 находок — `PASS`.
- Результат: `PASS`; дальнейшие релевантные проверки не будут пропускаться ради экономии лимитов.

### JF3-S0032 — Возобновление живого обхода установленной программы

- Управление окном JustFun 7.8.3 восстановлено через новый Computer Use session; обнаружено ровно одно окно установленной программы.
- Раздел `Заказы` повторно проверен: новая компания/склад содержат 0 заказов, 0 найденных записей и 0 ₽; фильтры, дата, статус, раздел, самовывоз и создание заказа видимы. Бизнес-данные не изменялись.
- Раздел `Рейсы по районам` открыт: все счётчики равны нулю; видимы фильтры даты/области, `Построить оптимальный план`, `+ Новый рейс`, статусы, диагностические показатели и вертикальная прокрутка. Бизнес-данные не изменялись.
- При переходе к `Товары` результат клика стал неопределённым из-за повторного `window capture timed out`; после повторного обнаружения окна восстановительный снимок также завершился timeout, поэтому дальнейшие входные действия в этом цикле остановлены без случайных повторных кликов.
- После сбоя тестового средства все 4 Electron-процесса отвечали, главное окно существовало, размер и время `desktop.log` не изменились. Дефект JustFun не подтверждён.
- Чистый log baseline: offset 729150 байт, строка 5598, последняя запись `2026-08-23T08:13:00.101Z`; все последующие действия сверяются только с приростом после этого маркера.
- Результат: `PARTIAL PASS`; проверены `Заказы` и `Рейсы`, продолжение требуется с нового session управления окном.

### JF3-S0033 — Установка актуальной полной сборки 7.8.3

- Полные GitHub-проверки ветки `codex/tz3-execution-baseline` завершились успешно: Audit incremental run `32631434485` — `PASS`, Windows native release gate run `32631434527` — `PASS`.
- Artifact `9491281647` (`justfun-windows-e2ebe417f37454e56fb8848c53e004f3c5e79bec`) скачан полностью; локальный SHA-256 ZIP `5a2bcbf1f6b852292998669e0871b1025a07d21e30c8ce81ff5db2ee39e99d40` совпал с GitHub digest.
- Перед установкой сохранена проверенная резервная копия текущих AppData: 17/17 основных файлов и 1/1 файл профиля совпали по SHA-256. Значения `native-secrets.json` не открывались.
- Пользователь самостоятельно удалил предыдущую установку; старый `OrdersLogistics.exe` перед новой установкой отсутствовал.
- Запущен основной `Orders-Logistics-Setup-7.8.3-Premium.exe`, SHA-256 `9faa6c163e70377a2bd0ca2c5211e994fc55e798dd71a61ed902d0cefbb3aa1b`; служебный Update Helper не использовался вместо установщика.
- Установщик завершил этапы `validate-targets`, `extract-stage`, `commit-atomic-install`, `write-config` и `register`; журнал содержит `SUCCESS`.
- Установленный `OrdersLogistics.exe` имеет SHA-256 `d0f2dbed32d57d82dfe2f86347f0c06fbbe32d0045d04599f46ca73e93da3783`, точно совпадающий с `BUILD-MANIFEST.json` актуального артефакта. FileVersion и ProductVersion равны `7.8.3`.
- Подтверждены режим `full`, отдельная папка рабочих данных, ярлык рабочего стола и ярлык меню «Пуск».
- Финальный экран установщика открыт на кнопке `Запустить JustFun`; первый запуск именно этой сборки ещё не выполнялся и требует отдельного подтверждения в момент действия.
- Результат: `INSTALL PASS`; первый запуск актуальной сборки — `PENDING CONFIRMATION`.

### JF3-S0034 — Первый запуск актуальной сборки и повторная авторизация

- После отдельного подтверждения владельца финальный экран установщика закрылся и запустил установленный `OrdersLogistics.exe`; найдено ровно одно окно `JustFun Логистика · 7.8.3`.
- Журнал подтвердил runtime hardening без ошибок, полную проверку защищённого ASAR и успешный запуск renderer; SHA-256 архива приложения текущей сборки: `DAC8D24BDA2357F7332D9E1053DE2120B0BE9BFE1D2AB8CE762D92240B6441DA`.
- Автоматическое восстановление ранее подтверждённой сессии владельца не прошло: `cloud auth state rejected` с сообщением `Защищённый локальный ключ повреждён или создан другим пользователем Windows`; итог `authorized=false`.
- Ошибка произошла после переустановки под тем же пользователем Windows. До удаления предыдущей установки журнал подтверждал `authorized=true` для зарегистрированного владельца.
- Содержимое `native-secrets.json` не открывалось. Метаданные показали, что после отклонения сессии файл был перезаписан: резервная копия 3512 байт, текущий файл 1256 байт; SHA-256 различаются.
- Открыт правильный сценарий `Войти по логину`; повторная активация новой компании не выполнялась. Код компании, логин и пароль владелец ввёл лично.
- Визуально стартовый экран и форма входа открылись без явных наложений или обрезания основных элементов.
- Повторный вход в существующую компанию прошёл успешно: форма авторизации скрылась, открылось рабочее пространство владельца активного склада.
- Результат: `STARTUP PASS / AUTH PERSISTENCE FAIL / RELOGIN PASS`.

### JF3-S0035 — Повторный вход, изоляция контекста и безопасный обход рабочих разделов

- После повторного входа открыта существующая компания владельца; новая компания и новая лицензия не создавались.
- В `Пользователи и права доступа` отображаются только владелец и один текущий подключённый компьютер. Старые сотрудники и устройства не появились.
- В `Склады и рабочее пространство` отображается только активный `Склад Санкт-Петербург`; данные другого склада не обнаружены.
- Начальное состояние активного склада: заказов `0`, водителей `0`, рейсов `0`. Встроенный каталог содержит 62 товара, но отслеживаемых остатков `0`, дефицита `0`, стоимости остатков `0 ₽`; это системный каталог, а не данные прежнего склада.
- Без сохранения и изменения данных визуально проверены вкладки `Заказы`, `Товары`, `Водители`, `Рейсы по районам`, `Отчётность`, `Настройки маршрута`, `Настройки программы`, раскрытие справки, внутренние прокрутки, закрытие по ESC и возврат фокуса.
- Центр обновлений показывает установленную `7.8.3`, доступную версию `—`, канал `Стабильный`, состояние `Не подключено`; кнопки проверки, загрузки и установки обновления действительно отключены.
- REG.RU показывает `Не настроен`: IP VPS пустой, SSH-пользователь `root`, порт `22`; проверка связи и резервного копирования без IP не запускалась.
- Telegram + Cloudflare показывает `Нужно восстановить`: бот и группа склада не подключены, общий профиль компании не сохранён, старый локальный ключ недоступен текущему Windows-пользователю. Изменение инфраструктуры не запускалось.
- OpenStreetMap использует стандартные адреса Nominatim, OSRM и тайлов; поля и кнопки отображаются без обрезания.
- Обнаружено визуальное расхождение общего заголовка только во вкладке `Настройки программы` — `JF3-DEFECT-003`.
- Результат: `PASS WITH OPEN DEFECTS`; безопасный обход выполнен, бизнес-сценарии с созданием данных продолжаются.

### JF3-S0036 — Непрерывный диагностический проход до единого пакета исправлений

- Решение владельца: во время живого тестирования не останавливаться на исправлении каждой найденной проблемы.
- Ошибки функций, кода, журналов, дизайна, оформления, размеров, прокрутки и логических несоответствий фиксируются с доказательствами и остаются `OPEN` до окончания полного доступного обхода.
- После дефекта продолжаются все независимые сценарии; заблокированным отмечается только фактически недоступный путь.
- Фиксируются также успешные шаги и полный фактический цикл, а не только ошибки.
- После первого прохода дефекты группируются по первопричинам, исправляются одним контролируемым пакетом, создаётся новая сборка и выполняется полный повторный живой тест.
- Результат: `TEST POLICY RECORDED`; текущая стадия остаётся обнаружением, продуктовые исправления сейчас не начинаются.

### JF3-S0037 — Создание главного полного ТЗ №3 коммерческого релиза

- Вся подтверждённая история проекта, существующие специализированные ТЗ, карта компонентов, исходные проверки, живой реестр и открытые дефекты сведены в один главный документ [`TZ3-COMPLETE-RELEASE-SPEC.md`](TZ3-COMPLETE-RELEASE-SPEC.md).
- Зафиксированы 38 разделов требований и восемь обязательных ворот `GO/NO-GO`.
- Сохранены решения владельца: подпись издателя Windows необязательна; подписанное автоматическое обновление обязательно; сравнение адресов с Яндекс Картами и локальные адресные архивы исключены; новая архитектура общего Telegram-бота отменена; используется текущая интеграция.
- Непрерывный discovery-проход, единый пакет исправлений и полный повторный живой тест закреплены как обязательная последовательность.
- Документ не меняет product source и не объявляет текущую сборку готовой.
- Результат: `SPECIFICATION RECORDED`; релизный статус остаётся `NO-GO` до доказанного прохождения всех ворот.

### JF3-S0038 — Автономный полный тест и обновление старых баз

- Владелец предоставил локальный тестовый набор доступов к программе, VPS, Cloudflare и двум Telegram-ботам. Секретные значения не перенесены в ТЗ, Git, журналы, команды или реестр.
- Зафиксировано требование: после однократного безопасного импорта тестовых секретов по псевдонимам весь сквозной прогон выполняется без участия владельца.
- В обязательную проверку включены scoped localStorage, IndexedDB, защищённые desktop-данные, PostgreSQL VPS V3, сервер лицензий, Company Telegram broker, Telegram Worker/D1 и формат Cloudflare KV каталога обновлений.
- Каждая заявленная поддерживаемая старая схема обязана автоматически пройти до текущей версии с проверкой таблиц, столбцов, ключей, индексов, RLS/policies, строк, IDs, связей, scope и digest.
- Неизвестная или повреждённая схема не изменяется: требуется backup, безопасная остановка и точный диагноз.
- Обязательны повтор, частичная миграция, аварийный обрыв, два клиента, несовместимый старый клиент, большая база и реальное восстановление backup.
- Полный контракт: [`UNATTENDED-E2E-AND-DATABASE-MIGRATION-SPEC.md`](UNATTENDED-E2E-AND-DATABASE-MIGRATION-SPEC.md).
- На этом шаге автоматический исполнитель, product source, базы и внешняя инфраструктура не изменялись; тестовый прогон и миграционная матрица ещё не выполнялись.
- Результат: `SPECIFICATION RECORDED / NOT EXECUTED`; релизный статус остаётся `NO-GO`.

### JF3-S0039 — Первый полный диагностический проход установленной 7.8.3

- Включена встроенная демонстрационная база: подтверждены 70 заказов, 62 товара, 18 водителей и 17 рабочих рейсов. Живые бизнес-данные намеренно не изменялись.
- Заказы: поиск, фильтр `Требует уточнения`, пагинация и карточка заказа открываются. Сохранение заполненного синтетического заказа в DEMO остановлено требованием рабочего VPS; заказ не создан.
- Адреса: точный запрос Санкт-Петербурга найден и разобран до региона/района; запрос с опечаткой `санкт петербуг невский 28` не найден. Журнал подтвердил использование публичного fallback Nominatim без настроенного серверного provider.
- Товары и склад: поиск `OS` вернул пять карточек ОСБ; карточка товара и форма движения открываются, предварительный остаток пересчитывается. Сохранение движения в DEMO также остановлено требованием VPS.
- Водители: открыты обычные водители и два агрегатора. Telegram-действие корректно сообщает об отсутствии настройки, но содержит технический префикс `Error:` (`JF3-DEFECT-005`).
- Рейсы: карточка, карта, последовательность точек, водитель и стадии отображаются. Фильтр `В пути (1)` не ограничил фактический список и пагинацию; код подтвердил конфликт stage-filter и pagination (`JF3-DEFECT-004`).
- Отчётность: недельный период `17.08.2026–23.08.2026` применился, показатели пересчитались без видимой ошибки.
- Настройки маршрута: открыты точка склада, правила маршрутов, интеллектуальный диспетчер, график погрузки и тарифы доставки. Карта после начальной задержки догрузилась; журнал содержит один `tile.openstreetmap.org / net::ERR_FAILED` при HTTP 200, поэтому это пока наблюдение, а не отдельный дефект.
- Настройки программы: проверены пользователи, склады, компания/логотип/документы, поведение, DEMO, обновление, резервные копии, REG.RU, Telegram и OpenStreetMap. В DEMO остались активны реальные приглашения и создание склада; сохранение склада не запускалось, потому что код направляет его в серверный LIVE-реестр (`JF3-BLOCKER-005`).
- Резервная копия и восстановление: экспорт выполнен, safety-backup создан; восстановленный снимок совпал с исходным по данным, кроме ожидаемых `exportedAt` и `warehouse.updatedAt`.
- Встроенная диагностика: `17 PASS / 17`, предупреждений и критических ошибок `0`; QA JSON экспортирован.
- Telegram + Cloudflare: профиль компании отсутствует/повреждён, бот и группа склада не подключены. Предыдущая живая проверка токена дошла до Telegram и завершилась `ETIMEDOUT 149.154.166.110:443`; рабочая интеграция не подтверждена.
- REG.RU: текущий отпечаток SSH не совпадает с сохранёнными записями, поэтому обход проверки запрещён до независимого подтверждения владельцем провайдера. Две предыдущие попытки установки завершились на `install.sh` кодом `2`; упакованный shell-файл имеет `CRLF` (`JF3-BLOCKER-004`).
- Центр обновлений: установленная версия `7.8.3`, каталог не подключён, доступная версия неизвестна, действия отключены; `JF3-BLOCKER-001` открыт.
- Журнал текущего запуска подтверждает целостность ASAR, runtime hardening, успешный renderer, Telegram-not-configured, Nominatim fallback, успешные загрузки backup/QA и единичный сбой тайла. Новых crash или renderer-fatal записей нет.
- Результат: `DISCOVERY PASS WITH 5 OPEN BLOCKERS`; независимые локальные экраны пройдены, но полный бизнес-цикл, VPS, Telegram, второй склад, второй пользователь/ПК, обновление и миграционная матрица заблокированы или не выполнены.

### JF3-S0040 — Первый единый пакет исправлений после диагностического прохода

- Коммит исходников `afb1821` отправлен в GitHub-ветку `codex/tz3-execution-baseline`.
- Установщик VPS теперь перед SFTP-записью переводит `install.sh` в UTF-8/LF и удаляет BOM; `.gitattributes` дополнительно закрепляет `eol=lf`.
- Временный сбой Windows DPAPI больше не удаляет зашифрованную авторизацию; отдельный тест подтвердил неизменность байтов и восстановление чтения той же сессии.
- Учебная среда отделена от лицензионной редакции: обычные DEMO-операции выполняются локально, а реальные пользователи, склады, VPS и Telegram не вызываются.
- Полный автоматический DEMO-цикл прошёл `69/69`; `cloudSyncUploads=0`, runtime errors `0`, во всём bridge-реестре был только локальный экспорт резервной копии.
- FULL atomic-mutation прошёл: подтверждённая операция дошла до серверного payload, отклонённая операция вернула `false`, локальные данные автоматически восстановлены.
- Фильтр стадий рейсов объединён с пагинацией; отдельный тест подтвердил 11 отфильтрованных рейсов на двух страницах и сброс на первую страницу при смене стадии.
- Общий заголовок использует единый активный branding; Telegram-ошибка водителя больше не показывает технический префикс `Error:`.
- Прошли: main unit, native SSH unit, current-cycle regression, route-stage pagination unit, release regression, entity/bootstrap reconciliation, REG entity contract, source hygiene, experience, visual contract и security regression.
- Новая Windows-сборка ещё не создана и не установлена; исходные дефекты имеют статус `SOURCE FIXED / LIVE RETEST REQUIRED`, релиз остаётся `NO-GO`.

### JF3-S0041 — Исправление подтверждённой опечатки в адресе без локальной базы

- Коммит `8cf881a` добавляет ограниченное алгоритмическое исправление опечаток в названиях приоритетных регионов; полный архив адресов в приложение не добавлялся.
- Запрос первого живого прохода `санкт петербуг невский 28` теперь преобразуется в `санкт петербург невский 28`; соседний корректный запрос `санкт павловск садовая 5` не изменяется.
- `requestedRegion()` после исправления распознаёт Санкт-Петербург и сохраняет региональный приоритет ранжирования.
- Unit-тест адресного интеллекта, main unit, release regression, security regression и source hygiene прошли.
- Реальный запрос к тому же публичному Nominatim-контракту вернул три результата; первым получен `Дом Зингера, 28, Невский проспект, Санкт-Петербург` с координатами.
- `JF3-BLOCKER-002` не закрыт: требуется новая сборка, живой повтор в приложении, production provider и большой эталонный корпус адресов.

### JF3-S0042 — Durable local-first для обычных данных без обязательного VPS

- Коммит `d512f89` добавляет локальную долговечную очередь изменений, разделённую по компании, среде и складу.
- Обычные заказы, товары, движения, водители и настройки сначала сохраняются на компьютере; недоступность VPS больше не отменяет локальное изменение.
- Каждая команда получает постоянный `command_id`, автора, устройство, исходные версии записей и состояния `pending/sending/confirmed/conflict/rejected`.
- После перезапуска прерванная отправка остаётся в очереди; повтор использует тот же `command_id`. Повреждённая очередь не перезаписывается и безопасно блокирует новые изменения.
- Server-wins bootstrap сначала получает подтверждённые серверные записи, затем накладывает только долговечные локальные намерения. Конфликт не уничтожает локальные данные и показывается пользователю.
- Критические переходы маршрута, выдача/списание и массовая очистка по-прежнему требуют подтверждения VPS и откатываются при отказе.
- FULL runtime: локальное удаление без сети сохранено, пережило повторное открытие очереди и bootstrap; после восстановления сети та же команда подтверждена со второй попытки и тем же `command_id`.
- OFFLINE runtime: локальное удаление выполнено и сохранено; критическая массовая очистка вернула `false`, данные остались на месте.
- DEMO runtime: `25/25` скриптов, `69/69` бизнес-проверок, runtime errors `0`, `cloudSyncUploads=0`.
- Прошли 13 контрольных тестов local-outbox/source-contract/bootstrap/background/release/security/source-hygiene/visual/experience/current-cycle/static/startup/atomic и отдельный `main-unit` Windows-слоя.
- Защищённая Windows-сборка с этим изменением впоследствии создана и установлена; полный живой connectivity-прогон ещё не начат из-за `JF3-DEFECT-006`. `JF3-BLOCKER-003` остаётся открыт до матрицы на физическом ПК и VPS.

### JF3-S0043 — Защищённая сборка, установочная приёмка и первый запуск

- Официальный build из коммита `bfdf126` прошёл release contract `82/82`, security audit `181 files / 0 findings`, source/static/main/updater/current-cycle, FULL runtime, DEMO `69/69`, visual/release и installer source `20/20`.
- Создан защищённый ASAR без loose app. SHA-256 payload EXE: `5bc573edc67c35128e1a5c8301a0d2fd27a041fd1e2ccd8107d4d6e256f72f49`; SHA-256 ASAR: `ae1c80a8a9962826df392a9622c8941bf96af38f73b79668eca677362a5d59c6`.
- Полная установочная приёмка сначала штатно остановилась, потому что JustFun была открыта. После обычного закрытия приложения повтор прошёл: setup `0`, uninstall `0`, пользовательские данные сохранены, временные каталоги удалены.
- Создан установщик `Orders-Logistics-Setup-7.8.3-Premium.exe`, SHA-256 `782fd1c7811edaeb37c74e65233ac4d177a13910e363408f9626c60e3e11422b`, и владельческий архив, SHA-256 `73138f275f6b17054c73f1033fad56d3b1818cf4bc113594264a043d08333025`.
- Сборка установлена в основной пользовательский каталог с сохранением прежнего data path. Хэши установленного EXE и ASAR совпали с манифестом; stage/backup не остались; installer smoke прошёл.
- Первый запуск подтвердил целостность, hardening и действующую авторизацию владельца, но завершился экраном `Доступ ограничен` из-за конфликта сохранённого DEMO с рабочим ядром (`JF3-DEFECT-006`). Живой бизнес-прогон этой сборки не объявлен выполненным.

### JF3-S0044 — Исправление повторного запуска с сохранённым DEMO

- Коммит `dc5e9c2` устраняет смешивание локального учебного режима и серверной LIVE-среды при подтверждении контекста склада.
- В DEMO старт больше не вызывает рабочий `setActiveWarehouse`; серверные интеграции, пользователи и складской реестр по-прежнему не изменяются.
- В LIVE обязательное подтверждение сохранено: regression-тест получил ровно один вызов `{warehouseId: warehouse-1, environment: live}`.
- Прошли experience, current-cycle, source hygiene, static audit, main unit, entity/bootstrap, REG entity, release и security `181 files / 0 findings`.
- Требуется новая защищённая сборка и живой повтор: закрыть программу в DEMO, открыть снова, вернуться в LIVE и только затем продолжить local-first/VPS/Telegram матрицу. Релиз остаётся `NO-GO`.

## Текущее решение

- Stage 0: `PASS`.
- Gate A: `PASS` для source/CI; реальный PostgreSQL и Windows installer gate подтверждены.
- Адресный provider-инкремент: `SOURCE PASS / LIVE PARTIAL`; точный адрес найден, запрос с простой опечаткой не найден, серверный provider не настроен.
- Удаление склада: `SOURCE + POSTGRESQL PASS`, production и multi-PC подтверждение не выполнены.
- Установка локальной защищённой сборки `bfdf126` на физический ПК: `PASS`; установочная приёмка, installer smoke и хэши установленного EXE/ASAR подтверждены.
- Первый запуск `bfdf126`: `FAIL`; сохранённая авторизация владельца прочитана успешно, но сохранённый DEMO ошибочно заблокировал подтверждение склада (`JF3-DEFECT-006`).
- Исправление повторного запуска: `SOURCE PASS` в `dc5e9c2`; новая сборка и живой повтор ещё обязательны.
- Полная живая проверка: `DISCOVERY PASS INCOMPLETE`; первый обход основных экранов завершён, подтверждены новые блокеры DEMO/VPS и дефект фильтра рейсов.
- VPS: `BLOCKED`; SSH trust требует независимого подтверждения, а упакованный `install.sh` не исполняется из-за `CRLF` (`JF3-BLOCKER-004`).
- Telegram: `RESTORE REQUIRED / NETWORK TEST FAIL`; бот и группа не подключены, проверка Bot API завершилась timeout.
- Живой адресный provider: `PARTIAL FAIL`; стандартные OSM endpoints присутствуют, точный адрес найден, typo-tolerance не прошёл.
- Четыре заказа, полный маршрутно-статусный цикл, удаление и обновление: `BLOCKED` подтверждёнными продуктными дефектами.
- Local-first без VPS: `SOURCE + INSTALLED PAYLOAD PASS / LIVE BLOCKED`; durable outbox присутствует в установленном ASAR, но живой сценарий ещё не дошёл до рабочего интерфейса из-за `JF3-DEFECT-006`.
- Второй склад, второй пользователь и второй компьютер: `NOT RUN`; запуск внутри DEMO остановлен из-за риска обращения к реальным cloud-admin данным.
- Автономный полный прогон и матрица миграций старых баз: требования зафиксированы, реализация и выполнение `NOT RUN`.
- Итоговый релизный статус: `NO-GO` до закрытия всех ворот ТЗ №3.

### JF3-S0045 — Защищённая пересборка после исправления повторного запуска

- В установленный продукт вошло исправление `dc5e9c2`; сборка выполнена из HEAD `8d73d83` ветки `codex/tz3-execution-baseline`.
- Установщик: `build/windows-7.8.3-8d73d83-20260824/Orders-Logistics-Setup-7.8.3-Premium.exe`, SHA-256 `D0D9B788C8CA282CBF1808F382AB309B2D07A973FD94588EDE1C51F14D168695`.
- Установленный EXE: SHA-256 `2BCF7DAE215FD386EE9558F3A422DBCF40B0D15E551DB47D8A149D2C77285CD2`; защищённый ASAR: `293F161174CDEC19E28160385EF04239570C978D5B04ED2DCA8CADB6F90010EC`.
- Установщик, защищённый payload, запуск с сохранённым DEMO и возврат в LIVE прошли. Экран `Доступ ограничен` больше не появился.
- `JF3-DEFECT-006` закрыт живым повтором на физическом ПК.
- Результат: `BUILD / INSTALL / STARTUP / DEMO-LIVE RETEST PASS`.

### JF3-S0046 — Четыре локальных заказа и адресный поиск без VPS

- Без настроенного VPS локально созданы и после перезапуска прочитаны четыре заказа: одна доставка по Санкт-Петербургу, один самовывоз, одна доставка в деревню и одна доставка в СНТ.
- `ORD-2026-0001`: запрос с опечаткой `санкт петербуг невский 28` вернул три варианта; выбран Дом Зингера. Расчёт: 35,5 км, доставка 4 100 ₽; товар 2 220 ₽; итог 6 320 ₽.
- `ORD-2026-0002`: самовывоз, товар 2 220 ₽, оплата отмечена полученной; история оплаты сохранилась.
- `ORD-2026-0003`: полный длинный адрес не найден, сокращённый `Новое Девяткино Главная 60` вернул два варианта с оценкой 85%; маршрут 54,1 км, доставка 5 900 ₽, итог 6 370 ₽.
- `ORD-2026-0004`: вымышленное СНТ не дало ложного результата; реальный запрос `СНТ Локомотив Кировский район Ленинградская область` дал один результат 93%; маршрут 53,6 км, доставка 5 900 ₽, итог 6 170 ₽.
- Обычные локальные изменения действительно сохраняются без VPS и переживают перезапуск. Это живое частичное подтверждение local-first и закрывает прежнюю полную блокировку обычных данных.
- Кнопка `Готов к выдаче` для самовывоза и `Начать комплектацию` для готовых рейсов воспроизводимо не меняют состояние — открыты `JF3-DEFECT-007` и `JF3-DEFECT-008`.
- Результат: `LOCAL-FIRST ORDERS PASS / CRITICAL STATUS TRANSITIONS FAIL`.

### JF3-S0047 — Водители, построение рейсов и отчётность

- Созданы три водителя: Алексей, Борис и Виктор. Для каждого применены реальные габариты, грузоподъёмность и схема 70 ₽/км в круг.
- Длина кузова Бориса изменена с 3 до 4 м; после этого два областных заказа объединены в рейс `Новое Девяткино → Мгинское городское поселение`.
- Расчёт объединённого рейса: 182 км, 3 ч 43 мин, Борис назначен автоматически, затраты 12 738,16 ₽, вместимость подтверждена, score 80, дорожный расчёт OSRM 100%.
- Самовывоз корректно исключён из доставки. Подготовлены два готовых рейса; начало комплектации обоих заблокировано дефектом кнопки.
- В маршрутах одновременно показаны `Требуют решения: 2` и KPI `Проблемы: 0`, что открыто как `JF3-DEFECT-009`.
- Отчёт за август показал 4 заказа, 2 220 ₽ получено и 21 080 ₽ открытой работы. CSV выгружен в `Экспорт/director_report_2026-08-01_2026-08-31.csv` и прочитан с диска.
- Текст отчёта оптимистично оценивает состояние при надёжности 50/100, 62 отклонениях и двух важных действиях — открыт `JF3-DEFECT-010`.
- Результат: `DRIVERS / ROUTE CALCULATION / REPORT EXPORT PASS WITH OPEN STATUS AND UX DEFECTS`.

### JF3-S0048 — Товары, остатки, резервная копия, восстановление и приглашение

- Создан товар `ТЗ3 Тестовый товар склада`, артикул `TZ3-STOCK-001`, закупка 100 ₽, продажа 150 ₽, контроль остатков включён.
- Приход `ТЗ3-ПРИХОД-001`: 10 шт., партия `TZ3-BATCH-001`; расход `ТЗ3-РАСХОД-001`: 2 шт. После перезапуска физический и доступный остаток равны 8 шт., стоимость запасов 800 ₽.
- Поиск по артикулу, карточки, список, таблица и импорт из заказов открыты; импорт корректно сообщил, что новых уникальных товаров нет.
- Создана резервная копия `СПБ_WORK_backup_2026-08-24.json`; встроенная проверка базы: 17 PASS, 0 предупреждений, 0 ошибок.
- Эта же копия восстановлена через интерфейс; перед восстановлением автоматически создана страховая копия. После перезапуска заказы, водители, изменение кузова и товарный остаток сохранились.
- Создано одноразовое приглашение оператора с ограниченными правами на активный склад. Код приглашения в журнал не копируется.
- После закрытия окна приглашение нельзя снова увидеть, отменить или проверить в списке — `JF3-DEFECT-011`.
- Полный вход приглашённого пользователя и проверка прав требуют отдельного Windows-профиля или второго ПК и ещё не выполнены.
- Результат: `INVENTORY / BACKUP / RESTORE / RESTART PASS; SECOND-USER LOGIN PENDING`.

### JF3-S0049 — Второй склад, обновления и внешние подключения

- Попытка создать независимый склад `ТЗ3 Тестовый склад Москва` локально завершилась отказом: `сервер или локальное хранилище не подтвердили операцию`.
- Поиск адреса Москвы и повторный поиск уже действующего склада Павловска одинаково завершились сообщением `Идентификатор адресного запроса повреждён`.
- Ручная точка на карте обратным геокодированием превратилась только в общее значение `Точка на карте`.
- Создание склада связано одной кнопкой `Сохранить склад и подключить Telegram`; отдельное локальное создание без Telegram/VPS отсутствует. Открыты `JF3-DEFECT-012` и `JF3-DEFECT-013`.
- Центр обновлений показывает 7.8.3, доступную версию `—`, канал `Стабильный`; проверка, загрузка и установка отключены. `JF3-BLOCKER-001` остаётся открытым.
- REG.RU показывает `Не настроен`; проверка связи правильно требует сначала выполнить установку VPS. Однако `Сохранить активный склад` без IP показало зелёное сообщение `Сервер прочитан` — `JF3-DEFECT-014`.
- Telegram не настроен: проверка webhook и подключение группы вернули понятный `TELEGRAM_NOT_CONFIGURED`; кнопка после ошибки восстановила рабочее состояние. Реальная настройка требует данных владельца и не выполнялась.
- Стандартные адреса Nominatim, OSRM и OSM tiles сохранены без ошибки.
- Результат: `LOCAL INTEGRATION UI PARTIAL PASS / SECOND WAREHOUSE AND UPDATER FAIL / VPS AND TELEGRAM PENDING CREDENTIALS`.

### JF3-S0050 — Настройки маршрутов, поведение программы и контрольный перезапуск

- Проверены пять блоков настроек маршрута: склад, правила рейса, интеллектуальный диспетчер, график погрузки и стоимость доставки. Аккордеоны, справка, прокрутка и сохранение отрисовываются без наложений.
- Подтверждены действующие параметры: возврат на склад, 8–11 ч, разгрузка 10–20 мин, максимум 12 точек, пробег 1 000 км; сбалансированный диспетчер; два погрузочных места; тариф 100 ₽/км + 500 ₽ за точку.
- Три сохранения без фактического изменения увеличили индикатор очереди с 5 до 8. Открыт `JF3-DEFECT-015`.
- После штатного закрытия и повторного запуска сохранились 4 заказа, 3 водителя, суммарный кузов 32,40 м³, товарный остаток 8 шт., стоимость 800 ₽ и очередь 8 операций.
- Сохранились пользовательские настройки: размер `Комфортный`, уплотнение больших списков, сбалансированная автоматизация, показ найденных проблем и объяснений решений.
- Контрол `Показывать индикатор автосохранения` не имеет видимого переключателя — `JF3-DEFECT-016`.
- Поле `Последняя копия` после копий и восстановления 24.08 всё ещё показывает `23.08.2026 21:10` — `JF3-DEFECT-017`.
- Выбор логотипа `По центру` сохраняется, но общий заголовок визуально остаётся слева; настройка возвращена в `Слева` — `JF3-DEFECT-018`.
- `desktop.log` подтвердил штатный запуск, целостность ASAR и рабочий LIVE-контекст. Ошибка поиска адреса склада и отказ создания склада в журнал не попали — `JF3-DEFECT-019`.
- Результат: `RESTART PERSISTENCE PASS / SETTINGS SAVE PARTIAL / 5 NEW DEFECTS CONFIRMED`.

## Актуальное решение после JF3-S0050

- Установка, запуск, DEMO/LIVE-разделение, четыре локальных заказа, три водителя, расчёт маршрутов, товарный учёт, экспорт, backup/restore и перезапуск: `LIVE PASS` в проверенном объёме.
- Обычная локальная работа без VPS доказана. Полный reconnect/conflict/server-wins цикл ещё не доказан.
- Критические статусы самовывоза и рейса: `LIVE FAIL`.
- Второй склад без VPS/Telegram: `LIVE FAIL`.
- Адресный поиск: typo и СНТ прошли частично; общий desktop request-id сломан в настройках склада, большой утверждённый корпус не выполнен.
- Обновление без переустановки: `NOT CONNECTED / BLOCKER OPEN`.
- Второй пользователь, второй Windows-профиль и второй ПК: `PENDING`.
- VPS, production PostgreSQL V3, резервирование на сервере, восстановление, legacy-миграции и Telegram: `PENDING` до доступа и отдельного живого прогона.
- Итоговый статус релиза: `NO-GO`. Основной локальный контур уже работает, но выпуск клиентам до закрытия P1-блокеров будет преждевременным.

### JF3-S0051 — Удаление временных локальных записей и контрольный перезапуск

- Создан, изменён и удалён временный товар `TZ3-DELETE-001`; очередь локальных операций последовательно зафиксировала создание, изменение и удаление.
- Создан и удалён временный водитель с отдельными тестовыми реквизитами.
- После штатного перезапуска оба удалённых объекта не вернулись: товаров снова 63, контролируемый остаток основного тестового товара 8 шт.; водителей снова 3.
- Очередь из 13 локальных операций пережила перезапуск. Данные четырёх заказов, основного товара и трёх водителей не изменились.
- Результат: `LOCAL CREATE / EDIT / DELETE / RESTART PASS`; серверное удаление и повторное чтение на втором ПК ещё не выполнялись.

### JF3-S0052 — Первый пакет исправлений по живому локальному проходу

- Найдены и исправлены исходные причины `JF3-DEFECT-007`, `008`, `012`, `014` и `015`; `JF3-DEFECT-013` исправлен для автономного создания/редактирования/архивации склада и отделения Telegram, но перенос local-to-server ещё не завершён.
- Единый `geocodeSearch()` теперь сам создаёт корректный request-id для любого экрана; main-процесс безопасно журналирует отклонённый IPC-запрос без полного адреса.
- Подготовка комплектации рейса и статуса `Готов к выдаче` переведена в долговечную локальную команду. Необратимые действия — выезд, закрытие рейса, окончательная выдача и массовая очистка — сохранили server-confirmed/fail-closed политику.
- Автономная компания может создать пустой локальный склад, редактировать и архивировать его, а также переключать склады без VPS. Telegram настраивается отдельно и больше не является обязательным продолжением создания склада. Безвозвратное удаление склада без серверной backup/lease-защиты по-прежнему запрещено.
- Синхронизация больше не показывает успешное чтение, если VPS не настроен; повторная запись неизменённых координат склада не обновляет технический timestamp.
- Новый FULL runtime-тест подтвердил: request-id корректен; повторный no-op не увеличивает outbox; второй склад создан с отдельной пустой базой; Telegram не навязан; runtime errors `0`.
- Прошли main/address/current-cycle/experience/entity-contract/local-outbox/bootstrap/background/atomic и FULL runtime `local-warehouse`, `local-first-offline`, `local-first-retry`, `atomic-mutation`.
- Результат: `SOURCE FIX + AUTOMATED RETEST PASS / PROTECTED BUILD AND LIVE RETEST REQUIRED`; релиз остаётся `NO-GO`.

### JF3-S0053 — Контролируемый перенос локальной компании на пустой VPS

- Реализован автоматический перенос всех локальных LIVE-складов и их раздельных данных на явно пустой, ранее не инициализированный VPS PostgreSQL V3.
- Перед отправкой каждый scoped-раздел и локальная очередь проверяются; повреждённый или изменившийся во время переноса источник останавливает операцию без удаления локальных данных.
- Журнал переноса хранит только состояние, отпечатки и стабильные `command_id`, но не бизнес-данные и не секреты. Повтор после обрыва использует те же команды и не создаёт дубли.
- Подключение к уже заполненному чужими данными VPS автоматически не объединяет и не скрывает самостоятельные локальные склады. Потеря ранее перенесённого серверного склада также блокирует автоматическое продолжение.
- После подтверждения всех пакетов локальные outbox-команды помечаются представленными серверным снимком, исходный активный склад восстанавливается, а локальные исходные записи сохраняются как точка возврата.
- Серверный реестр теперь возвращает `catalog_mode`, поэтому новый склад с намеренно пустым каталогом не получает стандартный каталог после переноса.
- FULL runtime подтвердил два склада, заказ первого склада, товар второго, сохранение пустого каталога, исходного активного склада и `0` runtime errors. Отдельный restart/replay подтвердил четыре повторные попытки без увеличения четырёх уже обработанных команд.
- Проверки синтаксиса, current-cycle, entity source contract, entity bootstrap и оба миграционных runtime-сценария прошли.
- Результат: `SOURCE FIX + AUTOMATED MIGRATION/RESUME PASS / PRODUCTION VPS, BACKUP AND LIVE RETEST REQUIRED`; миграции старых PostgreSQL-схем ещё не выполнены, релиз остаётся `NO-GO`.

### JF3-S0054 — Безопасное обновление старых PostgreSQL V1/V2 до V3

- Удалён разрушительный startup-путь, который безусловно удалял `warehouse_snapshots`, `workspace_entities`, `workspace_change_events` и `processed_commands`.
- Зарегистрированы только две фактически найденные в истории проекта схемы: V1 с цельным JSONB-снимком склада и V2 с отдельными сущностями/событиями/командами. Неизвестный набор столбцов получает `UNSUPPORTED_SCHEMA`, повреждённый scope, JSON, идентификатор или digest — `CORRUPT_SCHEMA`; изменение базы при этом откатывается транзакцией.
- Миграция защищена PostgreSQL advisory-lock, работает пакетами по 1 000 строк, проверяет исходные SHA-256 и канонический company/warehouse/environment scope. Для одного ключа V2 считается более новым источником, а V1 заполняет только отсутствующие записи, не возвращая удалённые V2-сущности.
- Каждая перенесённая запись получает V3 event/version/digest; исходные таблицы не удаляются, а атомарно переименовываются в read-only архивное семейство `legacy_v1_*_archive` / `legacy_v2_*_archive`. Старые результаты команд сохраняются в архиве, но не выдаются за V3-команды с неизвестным request hash.
- `schema_migrations` получил SHA-256 зарегистрированных переходов; `/health` публикует фактическую версию и машинно-читаемый список применённых миграций.
- VPS-установщик до изменения базы создаёт `pg_dump`, проверяет его через `pg_restore --list`, сохраняет SHA-256, а при последующей ошибке полностью восстанавливает прежнюю базу и прежний сервер.
- Добавлены unit-тесты конвертации, scope/digest/duplicate/unknown-schema fail-closed, source-контракты и отдельная PostgreSQL integration-матрица V1, V1+V2, повторного запуска и архивов.
- Фактически сейчас прошли 5 unit-тестов и все смежные REG/API/revision/source/release/security проверки. Два PostgreSQL integration-теста подготовлены, но в текущей Windows-среде корректно пропущены: тестовый DSN PostgreSQL не настроен.
- Результат: `SOURCE IMPLEMENTED + UNIT PASS / POSTGRESQL INTEGRATION, PRODUCTION BACKUP/RESTORE AND LIVE MIGRATION NOT RUN`; релиз остаётся `NO-GO`.

### JF3-S0055 — Второй пакет исправлений после полного локального живого прохода

- Коммит исходников `f4db9a8` отправлен в `origin/codex/tz3-execution-baseline`.
- Исправлены исходные причины `JF3-DEFECT-009`, `010`, `011`, `016`, `017`, `018` и `020`; журналирование `JF3-DEFECT-019` расширено, но централизованная живая сверка ещё не завершена.
- Рейсы используют единую модель готовности; отчёт директора не может быть зелёным при низкой достоверности или важных действиях; равный габарит товара и кузова разрешён с числовой причиной отказа для превышения.
- Владелец получает безопасный реестр приглашений и может отозвать ожидающий код. D1 migration 009 сохраняет старые приглашения и блокирует вход по отозванному/просроченному коду на уровне базы.
- Windows подтверждает backup только после атомарной записи, перечитывания и SHA-256; восстановление не начинается без подтверждённой страховочной копии.
- Восстановлены нативный checkbox автосохранения и применение позиций логотипа слева/по центру/справа. Общий style-ratchet прошёл без роста `!important`.
- Прошли синтаксис, security, main/license/schema/auth, design, release и installer gates; FULL runtime прошёл order-print, order-save, atomic mutation, local-to-server migration/restart и учебный бизнес-цикл `69/69` без runtime errors.
- PostgreSQL integration-тесты по-прежнему пропущены из-за отсутствия `JF_TEST_POSTGRES_ADMIN_DSN`. Cloudflare Worker с migration 009 ещё не опубликован. Защищённая Windows-сборка из `f4db9a8` ещё не создана и не установлена.
- Результат: `SOURCE FIX + AUTOMATED RETEST PASS / PRODUCTION MIGRATIONS, PROTECTED BUILD AND LIVE RETEST REQUIRED`; релиз остаётся `NO-GO`.

### JF3-S0056 — Живой локальный проход установленной сборки и проверка долговечности

- Проверена установленная JustFun `7.8.3` из CI-артефакта `jf-7.8.3-87c3619895c8`; SHA-256 установленного EXE `6694368D3D7BE1A3BC8E898B824011B71B286A23D6876867A085F80F852EDC42`, журнал подтвердил ASAR `776B398FAB086A7F03EBCFCF4C5CC5B8400BCABA4D85F76CB3D9286AC422FB4B` и `archiveHashVerified=true`.
- Создан и изолирован склад `Склад ТЗ3 — изоляция` (`39bcd203-0a96-4dd1-9f68-df333171493c`). После перезапусков сохранились 4 заказа на `10 400 ₽`, 4 товара, один складской остаток `11 шт.`, один водитель и настройки маршрута.
- Заказы покрыли доставку по точному адресу, самовывоз, ручную точку при не найденном адресе и реальное СНТ. Точный и ошибочный поиск Невского проспекта не сработал; реальное СНТ найдено одним результатом.
- Для `ORD-2026-0003` сохранён отображаемый адрес `Невский проспект 28 Санкт-Петербург`, но координаты и обратное геокодирование указывают на Чугунную улицу. Это опасное расхождение адреса и фактической точки маршрута (`JF3-DEFECT-022`).
- Импорт из заказов создал четыре товарные карточки. Для объёмного груза редактор каталога не предлагает `м³`, пустая единица молча отображается как `шт`, а объём/вес заказа остаются `0` при выборе машины. Зафиксирован `JF3-DEFECT-023`.
- Аккордеон `Параметры груза` в заказе не открывается мышью, Enter или Space; единица нового товара наследуется от предыдущего заказа. Зафиксирован `JF3-DEFECT-024`.
- Создан водитель `Водитель ТЗ3 Тестовый`, ГАЗ Газель NEXT, `9,72 м³`, `1,5 т`, `70 ₽/км`. Данные водителя и машины пережили перезапуск.
- Оптимизатор построил два рейса на три точки: один готовый `30 км / 2 099,57 ₽`, второй заблокирован занятым единственным водителем. Нажатие `Начать комплектацию` вернуло готовый рейс в черновик и сбросило рассчитанные показатели; повторный расчёт восстановил их, но после перезапуска оба рейса снова стали черновиками. Живой статус `JF3-DEFECT-008`: `FAIL / REGRESSION CONFIRMED`.
- Поиск заказа по клиенту вернул один результат; быстрый фильтр `Зафиксированы в рейсе` вернул три. Сброс фильтров восстановил четыре заказа.
- Печать заказа открывает системный диалог, но Windows сообщает `Это приложение не поддерживает предварительный просмотр`; фактическая печать не запускалась (`JF3-DEFECT-025`).
- Для `ORD-2026-0004` подтверждение оплаты показало `Оплата произведена`, но индикатор outbox остался `3`. После штатного закрытия и повторного запуска заказ снова показал `Ожидает оплаты`. В `desktop.log` нет записи об операции. Открыт критический `JF3-DEFECT-021`.
- Тариф доставки сохранён и математически использует `500 ₽ за точку + 100 ₽/км`; в карточке заказа нулевая дистанция показывается без отдельной строки платы за точку, поэтому сумма выглядит необъяснимой.
- Два контрольных запуска подтвердили integrity, hardening, авторизацию владельца и тот же LIVE warehouse context. На каждом запуске повторяются `cloud invitation list failed / NOT_FOUND`, восстановление Telegram компании и `TELEGRAM_NOT_CONFIGURED`.
- Результат: `LOCAL DATA PARTIAL PASS / PAYMENT AND ROUTE DURABILITY FAIL / RELEASE NO-GO`.

### JF3-S0057 — Матрица адресов деревень и проверка журнала

- В несохранённом заказе `ORD-2026-0005` проверены полные и сокращённые адресы реальных населённых пунктов. Заказ закрыт через `Отмена`, новые бизнес-данные не созданы.
- Не найдены: `Псковская область, Псковский район, деревня Родина`; `деревня Родина, Псков`; `Новгородская область, Новгородский район, деревня Сырково`; `деревня Сырково, Великий Новгород`; `Республика Карелия, Прионежский район, деревня Вилга`; полный адрес деревни Жуковка в Одинцовском округе.
- Сокращённый адрес с намеренными ошибками `Моссковская обл, Одинцовский р-н, дер Жуковка` неожиданно дал один правильный вариант с оценкой `92%`.
- `desktop.log` подтвердил для всего прохода публичный development fallback: `configured=false`, `queryCorrected=true`. Для найденной Жуковки parser diagnostic определил регион, но не район (`regionDetected=true`, `districtDetected=false`).
- Поведение непоследовательно: более точная запись отклоняется, а ухудшенная запись того же адреса проходит. Открыт `JF3-DEFECT-026`; `JF3-BLOCKER-002` остаётся открытым.
- Результат: `ADDRESS VILLAGE MATRIX FAIL / PUBLIC DEVELOPMENT FALLBACK CONFIRMED / RELEASE NO-GO`.

### JF3-S0058 — Создание, изменение, архив и резерв самовывоза с двумя перезапусками

- Создан временный самовывоз `ORD-2026-0005` из складского товара `Груз ТЗ3 №4`; количество изменено с 1 до 2, сумма с `1 200 ₽` до `2 400 ₽`. Создание и изменение пережили перезапуск.
- Заказ переведён в `Не актуален` с тестовой причиной; интерфейс сразу показал архив, активный список вернулся к 4 заказам и `10 400 ₽`.
- После первого штатного перезапуска заказ сам вернулся в активные со статусом `Комплектуется`; стало 5 активных заказов на `12 800 ₽`. Архив, причина и состояние `Не актуален` потеряны (`JF3-DEFECT-027`).
- Для воскресшего заказа выполнено `Готов к выдаче`. В текущем сеансе склад посчитал правильно: физически 11 шт., резерв 2 шт., доступно 9 шт.
- После второго штатного перезапуска заказ снова стал `Комплектуется`, а резерв исчез: физически 11 шт., резерв 0 шт., доступно 11 шт. UI дважды сообщил локальный успех до недолговечной записи.
- Карточка самовывоза одновременно пишет `Заказ не включается в маршруты доставки` и `Заказ зафиксирован в маршрутном листе`; открыт `JF3-DEFECT-028`.
- Индикатор outbox вырос с 4 до 5, но `desktop.log` содержит только штатные остановки/запуски и не содержит создания, изменения, архива, готовности, резерва или отказа с correlation-id. `JF3-DEFECT-019` подтверждён повторно.
- Результат: `CREATE/EDIT DURABLE / ARCHIVE, PICKUP READY AND RESERVATION DURABILITY FAIL / RELEASE NO-GO`.

### JF3-S0059 — Адаптивный интерфейс и функции отчёта директора

- Основные разделы просмотрены при ширине окна примерно 1366, 1280 и 1100 px. При 1366 px верхние панели и рабочие формы помещаются.
- При 1280 px в отчётности правый фильтр `Состояние работы` обрезается контейнером. При 1100 px шапка обрезает логотип, название, активный склад и `Создать заказ`, а навигация требует горизонтальной прокрутки и скрывает крайний раздел (`JF3-DEFECT-029`).
- Переход в `Товары` сохранил старую общую вертикальную позицию и открыл новый раздел с середины, без шапки и основных кнопок; `Home` восстановил начало (`JF3-DEFECT-030`).
- `Выгрузить полный отчёт` дважды создал корректные CSV по 8 499 байт в штатной папке `Документы/JustFun/Заказы и логистика/Экспорт`. Русский заголовок, версия 7.8.3, период, склад, достоверность и сумма незакрытых заказов `12 800` прочитаны обратно. `desktop.log` подтвердил оба `download finished` со `state=completed`.
- Сохранение неизменённой методики показало успех и не увеличило outbox `5`; живой no-op контроль `JF3-DEFECT-015` прошёл для этой формы.
- `Печатная сводка` открыла Microsoft Print to PDF, но снова показала отсутствие preview. Окно сохранения PDF первоначально осталось скрытым за системным диалогом и не попало в список окон; после обнаружения файл сохранён и проверен в `JF3-S0060`. `desktop.log` не содержит результата печати.
- Результат: `CSV EXPORT PASS / NO-OP SAVE PASS / RESPONSIVE LAYOUT PARTIAL FAIL / REPORT PRINT OUTPUT REQUIRES VISUAL VERIFICATION / RELEASE NO-GO`.

### JF3-S0060 — Фактическая печать отчёта в PDF и визуальная проверка

- Microsoft Print to PDF сохранил `TZ3-report-print-20260824.pdf` в штатной папке экспорта; размер `133 733` байта.
- Структурная проверка: один лист A4 `595,3 × 841,9 pt`, ошибок чтения нет. Текстовый слой отсутствует (`0` извлечённых символов), поэтому поиск и копирование текста невозможны.
- Рендер страницы подтвердил наличие логотипа, русских заголовков, сумм, трёх действий и блока рейсов. Страница не пустая.
- Критический дефект макета: весь отчёт повёрнут на 90° внутри книжного листа. Выбранная в Windows книжная ориентация не соответствует горизонтально сформированному содержимому; документ нельзя нормально читать или печатать без ручного поворота (`JF3-DEFECT-031`).
- Отсутствие preview остаётся `JF3-DEFECT-025`; фактическое создание PDF переводит проверку из `PRINT FILE FAIL` в `FILE CREATED / PRINT LAYOUT FAIL`.
- Результат: `PDF FILE CREATED / RUSSIAN CONTENT PRESENT / ROTATED LAYOUT AND NO TEXT LAYER FAIL / RELEASE NO-GO`.

### JF3-S0061 — Сотрудники и дополнительные расходы: полный CRUD, пересчёт и перезапуск

- В методике отчёта создан временный сотрудник `Сотрудник ТЗ3 Тестовый`: должность `Тестовый менеджер`, оклад `30 000 ₽/мес.`, начало работы `01.08.2026`.
- Создан разовый расход `Расход ТЗ3 Тестовый`: категория `Склад`, сумма `1 000 ₽`, дата `24.08.2026`, комментарий `Проверка ТЗ3`.
- Отчёт сразу и правильно включил обе записи: зарплата `30 000 ₽`, дополнительные расходы `1 000 ₽`, общий расход и убыток `31 000 ₽`.
- Оклад изменён на `31 000 ₽`, расход — на `1 100 ₽`. Отчёт без ручной перезагрузки правильно пересчитался до `32 100 ₽`.
- Обе тестовые записи удалены через отдельные подтверждения. Отчёт вернулся к нулевым расходам; сотрудник и расход исчезли из реестров.
- После штатного закрытия и повторного запуска JustFun удалённые записи не вернулись, нулевой результат отчёта сохранился. Создание, изменение, удаление, пересчёт и долговечность удаления прошли.
- `desktop.log` содержит штатные `application exiting`, новый boot, проверку целостности ASAR и восстановление того же LIVE-склада, но не содержит ни одной записи о создании, изменении или удалении сотрудника/расхода. Пробел наблюдаемости `JF3-DEFECT-019` подтверждён ещё для двух бизнес-сущностей.
- Результат: `EMPLOYEE AND EXPENSE CRUD/RECALCULATION/DELETE DURABILITY PASS / MUTATION AUDIT LOGGING FAIL / RELEASE NO-GO`.

### JF3-S0062 — Временный водитель и автомобиль: CRUD, доступность, поиск и перезапуск

- Создан `Водитель ТЗ3 Временный` с тестовым контактом, автомобилем `ТЗ3 Фургон Временный`, госномером `В000ТТ178`, кузовом `3,2 × 1,8 × 1,9 м`, полезным объёмом `10,94 м³`, грузоподъёмностью `800 кг` и тарифом `70 ₽/км в круг`.
- Сводка корректно изменилась с 1 до 2 исполнителей и с `9,72` до `20,66 м³`; outbox вырос с 5 до 6. Поиск по госномеру вернул только созданного водителя.
- Доступность изменена с активной на `Неактивен / временно недоступен`: активных исполнителей стало 1, карточка получила правильный статус, outbox вырос до 7.
- После удаления временного водителя общее количество вернулось к 1, объём — к `9,72 м³`, outbox вырос до 8. После штатного перезапуска удалённый водитель не вернулся, исходный водитель и его автомобиль не изменились.
- После удаления поисковая строка ещё содержала госномер. При нулевом результате интерфейс показал ложное пустое состояние `Исполнители не добавлены`, хотя сводка одновременно показывала одного исполнителя; открыт `JF3-DEFECT-032`.
- `desktop.log` содержит только штатное завершение и следующий запуск с integrity/warehouse evidence; создание, изменение доступности и удаление водителя не журналируются. `JF3-DEFECT-019` подтверждён повторно.
- Результат: `DRIVER CREATE/EDIT/SEARCH/DELETE DURABILITY PASS / FILTERED EMPTY STATE AND MUTATION AUDIT LOGGING FAIL / RELEASE NO-GO`.

### JF3-S0063 — Второй склад: изоляция, отдельные данные, outbox и перезапуск

- Создан отдельный локальный склад `Склад ТЗ3 B — изоляция` с кодом `B03` и идентификатором `5799ff6a-1081-404d-a2be-c10b9da938f7`. Точный поиск `Санкт-Петербург, Софийская улица, 60` вернул `Адрес не найден`; склад сохранён с ручной точкой `Точка на карте`, поэтому `JF3-DEFECT-012` подтверждён повторно.
- Сразу после открытия склад B был пустым: 0 заказов, 0 товаров, 0 водителей, 0 рейсов и нулевой отчёт. Ни одна запись склада A не отобразилась.
- На складе B создан товар `Товар ТЗ3 B` (`TZ3-B-001`, закупка `500 ₽`, продажа `700 ₽`) и приход `5 шт.` по документу `TZ3-B-REC-001`, партия `BATCH-B-001`. Сводка показала один товар, остаток `5 шт.`, стоимость запасов `2 500 ₽` и потенциальную маржу `200 ₽`.
- Создан отдельный самовывоз `ORD-2026-0001` на `700 ₽`, наличные, `Ожидает оплаты`, статус `Комплектуется`. Склад B показал ровно один заказ на `700 ₽`; данные склада A не появились.
- Outbox склада B вырос с 0 до 1 после товара и до 2 после прихода, но после сохранения заказа остался равен 2. После переключения и полного перезапуска заказ сохранился, а outbox всё ещё равен 2. Открыт `JF3-DEFECT-033`: долговечный заказ не поставлен в очередь серверной синхронизации.
- После штатного перезапуска JustFun автоматически восстановила склад B: один заказ на `700 ₽`, один товар, физический и доступный остаток `5 шт.`, резерв 0, стоимость `2 500 ₽`. Затем выполнен возврат на склад A.
- После возврата склад A не изменился: 5 заказов на `12 800 ₽`, 4 товара, один контролируемый остаток `11 шт.`, один активный водитель с кузовом `9,72 м³`, 2 черновых рейса на 3 точки общей стоимостью заказов `9 650 ₽`, outbox 8. Данные склада B в этих разделах отсутствуют.
- Переключение склада снова сохранило старую вертикальную позицию и открыло другой контекст с середины страницы; `JF3-DEFECT-030` подтверждён для смены склада.
- `desktop.log` подтвердил оба warehouse-id и штатную проверку целостности. Записей о создании склада, товара, прихода и заказа нет (`JF3-DEFECT-019`). Во время одного reload склада B облачные вызовы получили `INVALID_SESSION`; полный перезапуск восстановил `authorized=true` и обычный `NOT_FOUND`, поэтому открыт требующий повторения `JF3-DEFECT-034`.
- Результат: `WAREHOUSE DATA ISOLATION AND LOCAL RESTART DURABILITY PASS / ORDER OUTBOX AND TRANSIENT CLOUD SESSION FAIL / RELEASE NO-GO`.

### JF3-S0064 — Оформление компании и поведение программы

- В разделе `Компания, логотип и документы` проверены все положения группы бренда. `Слева`, `По центру` и `Справа` реально меняют её положение в общем заголовке; значение `Справа` сохранилось после полного перезапуска. Настройка возвращена в `Слева`. Повторный тест закрывает `JF3-DEFECT-018`.
- Флажок `В программе` снят и сохранён с сообщением об успехе, но логотип не исчез ни сразу, ни после полного перезапуска. Флажок при этом сохранился выключенным. Настройка возвращена во включённое состояние; открыт `JF3-DEFECT-035`.
- Без изменения данных осмотрены юридические, контактные и банковские поля, правила номера счёта и рекламный блок клиента. Текущий пример номера счёта — `ТЗ3-240826`; отдельная печатная проверка флажка `В документах` ещё требуется.
- В разделе `Поведение программы и автоматизация` стартовый раздел изменён на `Товары`, размер элементов — на `Компактный`. После перезапуска приложение открылось непосредственно в товарах и сохранило компактный режим. Затем значения возвращены в `Заказы` и `Комфортный`; второй перезапуск подтвердил восстановление.
- Карточка индикатора автосохранения успешно переключается и сохраняется, но не показывает нативный checkbox или галочку: состояние видно только по цвету рамки. Значение возвращено во включённое; открыт `JF3-DEFECT-036`. Работа самого индикатора во время мутации отдельно не доказана.
- Outbox склада A оставался равен 8 на протяжении всех сохранений и перезапусков. После восстановления настроек склад A сохранил 5 заказов на `12 800 ₽`.
- `desktop.log` подтвердил три штатных перезапуска, целостность ASAR, `archiveHashVerified=true`, hardening и восстановление того же warehouse-id. Записи о сохранении оформления и поведения отсутствуют, поэтому `JF3-DEFECT-019` остаётся открыт. Также сохраняются известные `NOT_FOUND` списка приглашений и `TELEGRAM_NOT_CONFIGURED`.
- Результат: `BRANDING POSITION AND BEHAVIOR PERSISTENCE PASS / LOGO VISIBILITY AND AUTOSAVE CONTROL UI FAIL / RELEASE NO-GO`.
