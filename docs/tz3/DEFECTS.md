# Дефекты и блокеры ТЗ №3

## Порядок обработки во время живой приёмки

Во время первого полного живого прохода дефекты только фиксируются с воспроизведением, видимым результатом и приростом журналов. Немедленные продуктовые исправления не выполняются. После регистрации проблема не останавливает независимые сценарии.

Исправления выполняются одним подготовленным пакетом после завершения всех доступных проверок. Затем каждый дефект и весь затронутый бизнес-путь проходят повторную живую проверку. Исключение для аварийного исправления допускается только по отдельному решению владельца, если приложение невозможно безопасно запустить и никакой другой сценарий проверить нельзя.

## Открытые релизные блокеры

### JF3-BLOCKER-001 — Автоматическое обновление не готово к реальному выпуску

| Поле | Значение |
|---|---|
| Тип | Release blocker |
| Приоритет | P1 до финального выпуска |
| Обнаружено | JF3-S0005 |
| Коммит | `b3a9e7c0f2892b4e3276b51ca5c0ee1760e88568` |
| Состояние | OPEN |
| GitHub | https://github.com/KAPCTEH/21.08reliz/issues/23 |

Подтверждённые факты:

- `source/application/update/policy.json`: `enabled=false`;
- endpoints internal/staging/stable равны `null`;
- allowlist хостов пуст;
- `source/application/update/trusted-keys.json`: `keys=[]`;
- `source/application/release.json`: `release_status=development`.

Частично исправлено коммитом `bdce184`:

- проверены живые staging и production Worker `/health`: оба отвечают HTTP 200, версия `1.0.0`;
- реальные endpoints и точные домены добавлены в update policy, но updater безопасно остаётся `enabled=false`;
- исправлена загрузка GitHub Release через ограниченный allowlisted HTTPS redirect;
- 223 проверки update/catalog прошли; security audit не нашёл нарушений;
- оригинальный Ed25519-ключ в разрешённой рабочей области и переменных окружения не найден.

Условие закрытия:

1. Подтверждён оригинальный ключ обновлений или проведена утверждённая первичная церемония доверия.
2. Публичный ключ встроен и проверен отрицательными тестами.
3. Staging endpoint и allowlist настроены.
4. Подписанный каталог и пакет проходят обновление и откат.
5. Stable включается только после canary.

### JF3-BLOCKER-002 — Поиск адресов не соответствует обязательному Stage 39

| Поле | Значение |
|---|---|
| Тип | Release blocker |
| Приоритет | P1 до финального выпуска |
| Обнаружено | JF3-S0012 |
| Коммит | `b3a9e7c0f2892b4e3276b51ca5c0ee1760e88568` |
| Состояние | OPEN |
| GitHub | https://github.com/KAPCTEH/21.08reliz/issues/24 |

Подтверждённое baseline-поведение:

- поиск запускается отдельной кнопкой;
- renderer запрашивает до 10 результатов Nominatim;
- основной и VPS fallback используют один класс данных Nominatim/OpenStreetMap;
- нормализация заменяет только несколько сокращений;
- ранжирование основано на простом вхождении слов и номера дома;
- отдельного ГАР/ФИАС-слоя и локального нечёткого индекса нет;
- confidence, контракт «до трёх достоверных вариантов», эталонный корпус и benchmark отсутствуют.

Частично исправлено коммитами `4ba860a25cceb2cef4a43c85ea25a0fc9cd030b3` и `ad4136d`:

- появились автоподсказки, расширенная нормализация, нечёткое ранжирование, предупреждения и Top-3;
- добавлены дедупликация, региональный приоритет и проверка номера дома/участка;
- добавлен изолированный тест и его обязательный запуск в Windows release gate;
- по прямому уточнению владельца полный ГАР/ФИАС-индекс отменён;
- добавлен scoped VPS-контракт поиска, adapter внешнего поставщика с ФИАС-кодами и координатами, временный кэш и безопасное хранение ключа;
- public Nominatim не используется для автоподсказок; без ключа доступен только явный поиск по кнопке;
- повреждённые ответы внешнего поставщика, неверные координаты и неверные ФИАС-коды отклоняются;
- коммитом `8cf881a` добавлено алгоритмическое исправление опечаток в названиях приоритетных регионов без локальной адресной базы; запрос `санкт петербуг невский 28` нормализуется в `санкт петербург невский 28`;
- автоматические тесты нормализации, регионального распознавания и безопасного public fallback прошли; отдельный сетевой запрос к используемому Nominatim-контракту вернул три варианта, первым — `Дом Зингера, 28, Невский проспект, Санкт-Петербург`;
- блокер не закрыт: реальный ключ поставщика не введён, production и большой корпус не проверены.

Условие закрытия:

1. Утверждена и реализована серверная архитектура поиска без локальной базы.
2. Подключён реальный разрешённый поставщик с ФИАС-кодами и координатами.
3. Реализованы нормализация, опечатки, сокращения, дедупликация и Top-3.
4. Созданы автоматические отрицательные и контрактные тесты.
5. Создан эталон минимум 2 000 адресов / 10 000 запросов.
6. Выполнены метрики и живые проверки приоритетных регионов.

## Открытые риски визуальной приёмки

### JF3-RISK-001 — Автоматический ratchet не доказывает единый стиль

- design-token regression проходит, но сообщает 1 191 уникальное HEX-значение, 62 размера шрифта, 52 радиуса и 130 теней в текущей области анализа;
- это не объявлено дефектом каждого значения без визуальной карты;
- требуется Stage 36 с инвентаризацией всех реально используемых значений и состояний.
- Состояние: `OPEN`.

## Подтверждённые дефекты

### JF3-DEFECT-001 — GitHub Release невозможно было скачать

| Поле | Значение |
|---|---|
| GitHub | https://github.com/KAPCTEH/21.08reliz/issues/25 |
| Обнаружено | JF3-S0018 |
| Исправлено | `bdce184` |
| Source status | FIXED / PASS |
| Live status | NOT TESTED |

Причина: загрузчик отклонял любой HTTP redirect, тогда как GitHub Release Assets официально может возвращать `302` на фактическое хранилище.

Исправление: разрешено не более трёх переходов только между явно разрешёнными HTTPS-доменами. Размер и SHA-256 по-прежнему обязательны. Переход на посторонний домен отклоняется тестом.

Живое обновление реального установленного приложения ещё не выполнено, поэтому Issue закрывать рано.

### JF3-DEFECT-002 — Авторизация владельца не сохраняется после переустановки

| Поле | Значение |
|---|---|
| Тип | Reinstall / protected-session defect |
| Приоритет | P1 до update/reinstall acceptance |
| Обнаружено | JF3-S0034 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |
| Source status | FIXED / AUTOMATED PASS (`afb1821`) |
| Live status | CONFIRMED ON PREVIOUS BUILD |

Подтверждённые факты:

- предыдущий RC после регистрации владельца запускался с `authorized=true`;
- пользователь удалил прежнюю установку, но AppData приложения сохранился и был отдельно скопирован перед новой установкой;
- актуальная сборка установлена под тем же пользователем Windows и совпала с CI-манифестом по SHA-256;
- при первом запуске новая сборка записала `cloud auth state rejected` и сообщение о повреждённом ключе или другом пользователе Windows, затем открыла экран авторизации;
- после отклонения сессии `native-secrets.json` стал меньше и изменил SHA-256; содержимое и значения секретов не открывались;
- бизнес-данные на этом шаге не изменялись, новая компания не создавалась.

Подтверждённая причина необратимой потери сессии в исходниках:

- `readNativeSecret()` перехватывает любое исключение `safeStorage.decryptString()` и уничтожает исходный код ошибки (`source/application/main.js`, строки 820–826);
- `readCloudAuthState()` затем на любое такое исключение безусловно удаляет `licenseCloudAuthV1` (`source/application/main.js`, строки 974–989);
- поэтому даже временная ошибка Windows DPAPI превращается в постоянный выход из аккаунта и уменьшение файла защищённых секретов;
- сам установщик и Update Helper защищённый профиль пользователя не удаляют. Предыдущий RC и текущая сборка используют одинаковые `main.js`, Electron 43.3.0 и installer-код, поэтому несовместимость формата между этими сборками не подтверждена;
- первоначальную причину отказа DPAPI доказать уже нельзя: код скрыл HRESULT, а затем удалил исходный шифротекст. Возможны повреждение шифротекста или несовпадение реального DPAPI-контекста.

Безопасный временный обход: войти по коду существующей компании, логину и паролю. Повторно активировать лицензию и создавать новую компанию нельзя.

Условие закрытия:

1. Воспроизвести переход build A → build B через переустановку и через Update Helper в изолированном профиле.
2. Присвоить сбою расшифровки отдельный код `NATIVE_SECRET_DECRYPT_FAILED`, сохранить исходную причину в безопасном журнале без значений секретов и не удалять шифротекст при временном сбое DPAPI.
3. Удалять сессию только при доказанно невалидном формате или auth-контексте; при временной недоступности хранилища корректно запросить обычный повторный вход без ложного сообщения о повреждении ключа.
4. Добавить автоматический regression test и повторить живую проверку под тем же Windows-пользователем.

Обязательный regression test: сохранить auth-сессию, заставить mock `decryptString()` один раз выбросить исключение, получить безопасный `null`, подтвердить неизменность байтов и SHA-256 хранилища, восстановить decrypt и прочитать ту же сессию без повторного входа. Дополнительно выполнить Windows acceptance `build A → uninstall keep-data → build B` и `build A → Update Helper → build B` под тем же SID.

Исправление исходников `afb1821`: временный отказ Windows secure storage и сбой расшифровки получили отдельные коды; зашифрованная сессия при таком сбое больше не удаляется. Автоматический тест подтвердил неизменность байтов и успешное чтение той же сессии после восстановления DPAPI. Живая переустановка новой сборки ещё не выполнена.

### JF3-DEFECT-003 — Заголовок программы меняет название только в настройках программы

| Поле | Значение |
|---|---|
| Тип | Visual consistency / branding |
| Приоритет | P2 |
| Обнаружено | JF3-S0035 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |
| Source status | FIXED / AUTOMATED SOURCE PASS (`afb1821`) |
| Live status | CONFIRMED ON PREVIOUS BUILD |

На рабочих вкладках общий заголовок показывает `JustFun Логистика` и подпись `ЗАКАЗЫ · СКЛАДЫ · МАРШРУТЫ`. После перехода только во вкладку `Настройки программы` тот же общий заголовок меняется на `Заказы и логистика` и `JUSTFUN · РАЗРАБОТКА И АВТОМАТИЗАЦИЯ ДЛЯ МАЛОГО БИЗНЕСА`. Геометрия страницы и элементы управления не ломаются, но единый стиль и название продукта нарушены.

Условие закрытия: выбрать один утверждённый вариант бренда для общего заголовка, использовать его на всех вкладках и добавить UI-regression test, который сравнивает заголовок после последовательного перехода по всем разделам.

Исправление исходников `afb1821`: общий desktop-слой больше не перезаписывает название жёсткой строкой и применяет единый активный профиль компании через `applyBranding()`. Полное визуальное сравнение вкладок остаётся обязательным после сборки.

### JF3-BLOCKER-003 — Обычные изменения полностью заблокированы без VPS

| Поле | Значение |
|---|---|
| Тип | Architecture / offline acceptance blocker |
| Приоритет | P1 до финального выпуска |
| Обнаружено | Живое сохранение первого синтетического заказа |
| Состояние | SOURCE FIXED / LIVE MATRIX BLOCKED BY `JF3-DEFECT-006` |
| Source status | FIXED / AUTOMATED LOCAL-FIRST PASS (`d512f89`) |
| Live status | REPLACEMENT BUILD INSTALLED / WORKSPACE STARTUP BLOCKED |

Подтверждённое поведение: в full-редакции guard `commitEntityMutation()` возвращает `false` до локальной мутации, если сессия offline, у компании отсутствует `data_service` или недоступен `regVps.syncEntities`. Поэтому обычный заказ не попадает даже в локальное хранилище, а пользователь получает сообщение о необходимости рабочего VPS.

Блокировка намеренно закреплена source-contract тестом, но противоречит интерфейсу и ТЗ №3: встроенная справка обещает продолжение работы с локальной базой и фоновое восстановление связи; живой acceptance требует создания допустимых локальных изменений без сети.

Условие закрытия:

1. Ввести классификацию `LOCAL_DURABLE`, `SERVER_CONFIRMED`, `CRITICAL_FAIL_CLOSED`.
2. Для обычных изменений реализовать durable outbox с `command_id`, scope, base version, состояниями и повторной отправкой.
3. Не уничтожать локально сохранённое намерение при server-wins bootstrap; конфликт показывать явно.
4. Оставить fail-closed только для критических общих переходов состояния.
5. Выполнить полную матрицу [`LOCAL-FIRST-CONNECTIVITY-ACCEPTANCE.md`](LOCAL-FIRST-CONNECTIVITY-ACCEPTANCE.md) до и после подключения VPS/Telegram.

Исправление исходников `d512f89`: обычные изменения сначала сохраняются в изолированной локальной очереди с постоянным `command_id`, company/warehouse/environment scope, исходной версией записи и состояниями `pending/sending/confirmed/conflict/rejected`. Перезапуск возвращает незавершённую отправку в `pending`; server-wins bootstrap накладывает только сохранённые локальные намерения; сетевой повтор использует тот же `command_id`; конфликт и окончательный отказ не удаляют локальные данные. Критические операции маршрутов, выдачи и массовой очистки остаются fail-closed. Автоматические сценарии подтвердили сохранение без VPS, переживание перезапуска, overlay после bootstrap, повтор после обрыва сети, rollback окончательно отклонённой команды и отсутствие VPS-вызовов в DEMO. Защищённая сборка `bfdf126` создана, прошла установочную приёмку и установлена на физический ПК; живая матрица ещё не начата из-за отдельного дефекта повторного запуска DEMO (`JF3-DEFECT-006`).

### JF3-RISK-002 — Общие тестовые VPS/D1 могут содержать legacy-схемы и данные

| Поле | Значение |
|---|---|
| Тип | Multi-tenant migration / data isolation risk |
| Приоритет | P1 до production onboarding |
| Состояние | OPEN RISK, LIVE INVENTORY NOT RUN |
| Source status | PARTIAL FOUNDATIONS CONFIRMED |

В текущем коде присутствуют schema migrations, PostgreSQL RLS, company/warehouse/environment scope и Telegram D1 migrations. Однако общие тестовые базы ещё не инвентаризированы на старые schema versions, orphan rows, legacy wildcard Telegram state и незавершённые операции. Поэтому наличие фактического конфликта пока не объявляется доказанным дефектом.

Условие закрытия: выполнить inventory без секретов, сохранить backup, применить и повторить идемпотентные миграции, проверить legacy quarantine, смешанные версии клиентов, cross-tenant negative tests и матрицу [`MULTI-TENANT-DATA-MIGRATION-SPEC.md`](MULTI-TENANT-DATA-MIGRATION-SPEC.md).

### JF3-BLOCKER-004 — Установщик VPS получает Windows-окончания строк и завершается до установки

| Поле | Значение |
|---|---|
| Тип | Windows packaging / VPS provisioning blocker |
| Приоритет | P1 до живой VPS-приёмки |
| Обнаружено | JF3-S0039 |
| Состояние | SOURCE FIXED / REBUILD AND LIVE RETEST REQUIRED |
| Source status | FIXED / UNIT PASS (`afb1821`) |
| Live status | CONFIRMED TWICE ON PREVIOUS BUILD |

Живой журнал двух последовательных попыток содержит `install.sh: line 2: set: pipefail : invalid option name` и завершение с кодом `2`. Рабочая база на VPS не удалялась.

Подтверждённая причина: `native-ssh.cjs` отправляет локальный `install.sh` через `fastPut` без нормализации. В установленном `app.asar` файл содержит `CRLF`, поэтому Linux `bash` получает скрытый символ `\r` в строке `set -euo pipefail`. Исходный файл в текущем checkout содержит `LF`, но в репозитории нет `.gitattributes`, а релизный путь не проверяет байты упакованного скрипта.

Условие закрытия:

1. Перед SFTP-записью преобразовывать только текст `install.sh` в UTF-8 с `LF` независимо от checkout и платформы сборки.
2. Зафиксировать `eol=lf` для shell-файлов в `.gitattributes` как дополнительную защиту.
3. Добавить тест упакованного `app.asar`, который запрещает BOM и `CRLF` в `install.sh`.
4. Повторить реальную установку на VPS и подтвердить TLS, schema V3, health, backup/restore и повторную идемпотентную установку.

Исправление исходников `afb1821`: перед SFTP-записью `install.sh` принудительно переводится в UTF-8/LF и очищается от BOM; `.gitattributes` закрепляет `eol=lf`; unit test запрещает прежний `fastPut` для shell-файла и проверяет CRLF/BOM-нормализацию. Упакованный ASAR и реальный VPS ещё не перепроверены.

### JF3-BLOCKER-005 — Учебный DEMO использует неправильный признак среды и не является автономно безопасным

| Поле | Значение |
|---|---|
| Тип | Demo isolation / environment routing blocker |
| Приоритет | P1 до коммерческого выпуска |
| Обнаружено | JF3-S0039 |
| Состояние | SOURCE FIXED / LIVE ISOLATION RETEST REQUIRED |
| Source status | FIXED / 69 OF 69 DEMO CHECKS PASS (`afb1821`) |
| Live status | CONFIRMED ON PREVIOUS BUILD |

Живой тест в режиме `Демонстрация программы` подтвердил, что сохранение нового заказа и складского движения останавливается сообщением о необходимости рабочего VPS. Одновременно в DEMO остаются активны реальные административные действия: создание приглашения пользователя и создание склада.

Подтверждённая причина: бизнес-среда определяется через `TeplitsaWarehouseBootstrap.isDemo()`, но серверные guards и административные панели проверяют только `desktopSession.edition === 'demo'`. У владельца полная редакция, поэтому включённая учебная база ошибочно рассматривается как LIVE. Создание склада дополнительно использует жёстко заданную серверную среду `live`.

Условие закрытия:

1. Развести понятия лицензионной редакции и текущей рабочей среды отдельными функциями и типами.
2. Разрешить обычные учебные изменения в локальном DEMO без VPS и не включать их в LIVE outbox.
3. В DEMO скрыть или заблокировать реальные cloud-admin операции пользователей, устройств, складов и интеграций либо предоставить явно отдельные безопасные симуляторы.
4. Добавить отрицательные тесты: ни один DEMO-сценарий не меняет LIVE localStorage/IndexedDB, PostgreSQL, license API, Telegram/D1 или реестр складов.
5. Повторить полный учебный цикл и сравнить хэши LIVE до/после.

Исправление исходников `afb1821`: лицензионная редакция отделена от активной среды данных; обычные учебные мутации остаются локальными, а реальные склады, пользователи, VPS и Telegram в DEMO не читаются и не изменяются. Автоматический полный DEMO-цикл прошёл `69/69`; список bridge-вызовов содержал только локальное скачивание резервной копии и не содержал VPS, Telegram или license API. Хэши реального LIVE-профиля после установки новой сборки ещё не сравнены.

### JF3-DEFECT-004 — Пагинация рейсов отменяет фильтр по стадии

| Поле | Значение |
|---|---|
| Тип | Route UI / filtering consistency |
| Приоритет | P1 для диспетчерского интерфейса |
| Обнаружено | JF3-S0039 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |
| Source status | FIXED / UNIT PASS (`afb1821`) |
| Live status | CONFIRMED ON PREVIOUS BUILD |

После выбора `В пути (1)` вкладка подсвечивается, но список и подпись пагинации продолжают учитывать все 17 рейсов; при переходе по страницам снова показываются рейсы других стадий.

Причина: `applyRouteStageFilterV560()` устанавливает `card.hidden` по стадии, после чего `94-route-workspace-final.js::applyPagination()` повторно устанавливает `card.hidden` только по номеру страницы и считает все карточки. Таким образом пагинация перезаписывает результат фильтра.

Условие закрытия: пагинация должна получать уже отфильтрованный набор, считать только его, не снимать stage/date/region filters и сбрасывать страницу в допустимый диапазон. Нужен UI-тест для каждой стадии на наборе больше десяти рейсов.

Исправление исходников `afb1821`: пагинация считает только карточки активной стадии, сначала скрывает полный набор и сбрасывается на первую страницу при смене фильтра. Новый JSDOM-тест подтвердил 11 рейсов на двух страницах, отсутствие карточек чужой стадии и корректный переход на набор из трёх рейсов.

### JF3-DEFECT-005 — Ошибка Telegram водителя содержит технический английский префикс

| Поле | Значение |
|---|---|
| Тип | Localization / error presentation |
| Приоритет | P3 |
| Обнаружено | JF3-S0039 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |
| Source status | FIXED / SOURCE PASS (`afb1821`) |
| Live status | CONFIRMED ON PREVIOUS BUILD |

При попытке Telegram-действия из карточки водителя пользователь получает понятное русское объяснение, но строка начинается с технического `Error:`. Условие закрытия: показывать локализованное пользовательское сообщение без имени JavaScript-класса, сохраняя код и техническую причину только в безопасном журнале диагностики.

Исправление исходников `afb1821`: пользовательское сообщение очищается от технического префикса, а исходная ошибка остаётся доступной диагностическому слою. Живой Telegram-путь новой сборки не проверен.

### JF3-DEFECT-006 — Сохранённый учебный режим блокирует повторный запуск полной версии

| Поле | Значение |
|---|---|
| Тип | Startup / environment isolation |
| Приоритет | P1 до продолжения живой приёмки |
| Обнаружено | JF3-S0043 |
| Состояние | SOURCE FIXED / REBUILD AND LIVE RETEST REQUIRED |
| Source status | FIXED / REGRESSION PASS (`dc5e9c2`) |
| Live status | CONFIRMED ON INSTALLED BUILD `bfdf126` |

После штатного закрытия программы в учебном режиме новая установленная сборка при повторном запуске показывала `Доступ ограничен / Склад не назначен`. Журнал подтвердил: авторизация владельца действительна, целостность ASAR подтверждена, но renderer отправил `environment=demo` в IPC подтверждения рабочего склада, тогда как защищённое ядро полной редакции принимает только `live`.

Причина: локальный учебный контур полной редакции намеренно хранится как DEMO, но `confirmActiveWarehouseContext()` безусловно вызывал рабочий bridge до монтирования интерфейса. Это смешивало признак локальной учебной базы с серверной средой текущей лицензии.

Исправление `dc5e9c2`: учебный контур больше не запрашивает рабочее серверное подтверждение при старте. LIVE-путь сохранил обязательный IPC-вызов с активным складом и `environment=live`. Regression-тест исполняет оба сценария: DEMO делает ноль bridge-вызовов, LIVE делает ровно один корректный вызов. Живое закрытие требует новой защищённой сборки, обновления установленного приложения и повторного запуска с сохранённым DEMO.

Живой повтор в защищённой сборке из `8d73d83` пройден: приложение запущено с сохранённым DEMO, затем возвращено в LIVE без экрана `Доступ ограничен`. `Live status: FIXED / PASS`, дефект закрыт.

### JF3-DEFECT-007 — Самовывоз нельзя перевести в состояние «Готов к выдаче»

| Поле | Значение |
|---|---|
| Тип | Pickup lifecycle / status transition |
| Приоритет | P1 |
| Обнаружено | JF3-S0046, `ORD-2026-0002` |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |

Кнопка `Готов к выдаче` доступна, но повторные нажатия не меняют статус, не добавляют запись истории и не показывают причину отказа. Оплата этого же заказа сохраняется нормально. Условие закрытия: трассировать UI → action dispatcher → critical mutation → ответ bridge/server → reread, показать явный успех или отказ, затем пройти полный цикл `создан → комплектуется → готов → выдан` с перезапуском.

### JF3-DEFECT-008 — Готовый рейс нельзя начать комплектовать

| Поле | Значение |
|---|---|
| Тип | Route lifecycle / critical transition |
| Приоритет | P1 |
| Обнаружено | JF3-S0047 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |

Для двух готовых рейсов кнопка `Начать комплектацию` визуально нажимается, но стадия, история и карточка не меняются; причина не показана. Условие закрытия: единый явный контракт подтверждения критической операции, корректное сообщение при отсутствии VPS, запись в журнал и полный маршрутный цикл до закрытия рейса.

### JF3-DEFECT-009 — Рейсы одновременно показывают две разные оценки проблем

| Поле | Значение |
|---|---|
| Тип | Route dashboard / contradictory metrics |
| Приоритет | P2 |
| Обнаружено | JF3-S0047 |
| Состояние | OPEN |

Один экран показывает `Требуют решения: 2`, а соседний KPI — `Проблемы: 0`. Пользователь не может понять, есть ли реальный блокер. Условие закрытия: определить одну модель состояний, различить черновики, предупреждения и ошибки, добавить согласованный счётчик и UI-тест на одинаковом наборе рейсов.

### JF3-DEFECT-010 — Текст отчёта противоречит фактическим показателям риска

| Поле | Значение |
|---|---|
| Тип | Reporting / decision accuracy |
| Приоритет | P2 |
| Обнаружено | JF3-S0047 |
| Состояние | OPEN |

Отчёт использует оптимистичную итоговую формулировку при надёжности 50/100, 62 отклонениях и двух важных действиях. Числа и CSV формируются, но управленческий вывод вводит в заблуждение. Условие закрытия: формализовать пороги красный/жёлтый/зелёный, генерировать вывод только из тех же метрик и закрепить граничные тесты.

### JF3-DEFECT-011 — Созданное приглашение пользователя нельзя просмотреть или отменить

| Поле | Значение |
|---|---|
| Тип | User administration / invitation lifecycle |
| Приоритет | P2 |
| Обнаружено | JF3-S0048 |
| Состояние | OPEN |

После создания одноразовый код показывается один раз, но в панели нет списка ожидающих приглашений, срока, статуса использования, кнопки отзыва или повторного создания. Условие закрытия: безопасный реестр приглашений без показа исходного секрета, статусы `ожидает/использовано/истекло/отозвано`, отзыв владельцем, аудит и живой вход приглашённого пользователя.

### JF3-DEFECT-012 — Поиск адреса склада отвергается как повреждённый запрос

| Поле | Значение |
|---|---|
| Тип | Desktop IPC / address search |
| Приоритет | P1 |
| Обнаружено | JF3-S0049 и JF3-S0050 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |

И создание нового склада в Москве, и поиск уже сохранённого адреса `Павловск, Санкт-Петербург` заканчиваются: `Error invoking remote method 'desktop:address-search': Error: Идентификатор адресного запроса повреждён.` Обычный поиск адреса внутри заказа при этом работает. Условие закрытия: сравнить формат request-id обоих renderer-путей с IPC-валидатором, использовать один генератор идентификаторов и пройти positive/negative IPC-тесты и живой повтор.

### JF3-DEFECT-013 — Второй склад невозможно создать локально и операция связана с Telegram

| Поле | Значение |
|---|---|
| Тип | Multi-warehouse / local-first architecture |
| Приоритет | P1 |
| Обнаружено | JF3-S0049 |
| Состояние | SOURCE FIXED / PRODUCTION VPS AND LIVE RETEST REQUIRED |

Кнопка называется `Сохранить склад и подключить Telegram`; без VPS операция завершается `сервер или локальное хранилище не подтвердили операцию`. Это противоречит выбранной архитектуре, где клиент может сначала работать локально и позже подключить сервер. Условие закрытия: отдельно создать локальный склад, отдельно подключать VPS и Telegram, автоматически переносить scoped-данные при выборе сервера и доказать пустой новый склад, изоляцию и переключение после перезапуска.

### JF3-DEFECT-014 — Не настроенный VPS показывает ложное успешное чтение сервера

| Поле | Значение |
|---|---|
| Тип | VPS state / misleading success message |
| Приоритет | P1 |
| Обнаружено | JF3-S0049 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |

При пустом IP и состоянии `Не настроен` команда `Сохранить активный склад` вывела зелёное сообщение `Сервер прочитан, но 5 локальных изменений ещё требуют отправки или решения`. Условие закрытия: запрещать состояние server-read без проверенной конфигурации/связи, разделить локальный snapshot и серверный ответ, вывести точный код причины и покрыть тестом `VPS missing`.

### JF3-DEFECT-015 — Сохранение неизменённых настроек создаёт лишние команды синхронизации

| Поле | Значение |
|---|---|
| Тип | Local outbox / idempotency |
| Приоритет | P1 |
| Обнаружено | JF3-S0050 |
| Состояние | SOURCE FIXED / LIVE RETEST REQUIRED |

Три нажатия сохранения без изменения значений увеличили очередь с 5 до 8. Без VPS пользователь получает бесконечно растущий индикатор за операции, которые ничего не меняют. Условие закрытия: сравнивать канонический payload с сохранённой версией, не создавать команду при no-op, дедуплицировать эквивалентные pending-команды и проверить перезапуск/reconnect.

### JF3-DEFECT-016 — У индикатора автосохранения отсутствует видимый переключатель

| Поле | Значение |
|---|---|
| Тип | UI control / accessibility |
| Приоритет | P2 |
| Обнаружено | JF3-S0050 |
| Состояние | OPEN |

Карточка `Показывать индикатор автосохранения` есть, но checkbox/switch не виден и не доступен через accessibility tree. Условие закрытия: настоящий подписанный контрол с focus/keyboard/checked состоянием, единый стиль и сохранение настройки после перезапуска.

### JF3-DEFECT-017 — Дата последней резервной копии не обновляется

| Поле | Значение |
|---|---|
| Тип | Backup UI / stale state |
| Приоритет | P2 |
| Обнаружено | JF3-S0048 и JF3-S0050 |
| Состояние | OPEN |

После создания двух копий и восстановления 24.08 поле продолжает показывать `23.08.2026 21:10`. Сами файлы созданы и читаются. Условие закрытия: обновлять подтверждённый timestamp только после успешной записи, reread при открытии настроек и перезапуске, различать ручную, страховую и серверную копии.

### JF3-DEFECT-018 — Настройка положения логотипа не отражается в общем заголовке

| Поле | Значение |
|---|---|
| Тип | Branding / visual settings |
| Приоритет | P2 |
| Обнаружено | JF3-S0050 |
| Состояние | OPEN |

Значение `По центру` сохраняется, но логотип в рабочем заголовке остаётся слева при включённом показе `В программе`. После проверки значение возвращено в `Слева`. Условие закрытия: один источник branding/layout, визуальные снимки для всех позиций и проверка всех вкладок/масштабов.

### JF3-DEFECT-019 — Отказы адреса и создания склада отсутствуют в desktop.log

| Поле | Значение |
|---|---|
| Тип | Observability / supportability |
| Приоритет | P1 |
| Обнаружено | JF3-S0049 и JF3-S0050 |
| Состояние | SOURCE PARTIAL FIX / LIVE LOG RETEST REQUIRED |

Пользователь получил два функциональных отказа, но журнал записал только успешный reverse-geocode. Без записи кода, scope, операции и безопасной причины удалённая поддержка не сможет диагностировать проблему. Условие закрытия: централизованно логировать каждый отказ IPC/business mutation без секретов и персональных значений, присваивать correlation-id и проверять совпадение UI-кода с журналом.

### JF3-DEFECT-020 — Груз длиной ровно как кузов помечается несовместимым без объяснения допуска

| Поле | Значение |
|---|---|
| Тип | Capacity rules / operator clarity |
| Приоритет | P2 |
| Обнаружено | JF3-S0047 |
| Состояние | OPEN |

Трёхметровый товар не помещается в кузов длиной ровно 3 м; после изменения кузова на 4 м рейс строится. Возможно, код намеренно учитывает запас, но интерфейс не показывает требуемый допуск и выглядит как ошибка. Условие закрытия: явно закрепить формулу полезной длины/запаса, показывать числовую причину несовместимости и тестировать границы `<`, `=` и `>`.

## Актуализация JF3-BLOCKER-002 — Живые адресные результаты новой сборки

- Исправленная опечатка Санкт-Петербурга теперь реально возвращает три варианта; первый пригоден для заказа.
- Реальное СНТ найдено, вымышленное не дало ложного совпадения.
- Длинная полная запись Нового Девяткино не найдена, сокращённая дала два варианта 85%.
- Поиск из настроек склада полностью блокируется `JF3-DEFECT-012`.
- Поэтому статус адресного блока остаётся `PARTIAL LIVE PASS / RELEASE BLOCKER OPEN`; production provider и утверждённый массовый корпус ещё не проверены.
