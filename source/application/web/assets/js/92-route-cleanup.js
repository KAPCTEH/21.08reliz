
(function(){
  'use strict';
  function cleanupRouteUiV591(){
    document.querySelectorAll('#tripsView .route-operational-links,#tripsView .route-execution-timing,#tripsView .route-ai-insight,#routeServiceStatusV560').forEach(el=>el.remove());
    document.querySelectorAll('#tripsView .route-lock-banner').forEach(el=>el.remove());
    if(settings.routeHintsEnabled!==true)document.querySelectorAll('#tripsView .route-readiness-panel').forEach(el=>el.remove());
  }
  // The final workspace pass invokes cleanup together with pagination. One
  // post-render traversal is enough and avoids moving the map repeatedly.
  window.cleanupRouteUiV591=cleanupRouteUiV591;
  window.JustFunOverrides.wrap('renderSettings','route-cleanup-v591',renderSettingsV591=>function(){const result=renderSettingsV591?.apply(this,arguments);if($('routeHintsEnabled'))$('routeHintsEnabled').checked=settings.routeHintsEnabled===true;return result});
  updateRouteServiceStatusV560=function(){$('routeServiceStatusV560')?.remove()};
  routeOperationalLinksV560=function(){return''};
  routeExecutionTimingV560=function(){return null};

  printRoute=function(id){
    const def=routeState().allDefs.find(d=>d.id===id),plan=def?validRoutePlan(def):null;if(!def||!plan||plan.finalized===false)return;
    const ordered=asArray(plan.orderedIds).map(oid=>def.orders.find(o=>o.id===oid)).filter(Boolean),driver=assignedDriverForRoute(def.id),rules=routeRuleMetrics(plan,ordered.length),paymentInfo=rules.paymentInfo||{total:0},returns=routeReturnsToWarehouse(),printedAt=formatDateTime(new Date().toISOString()),slot=routeLoadingSlot(def),adjusted=adjustedRouteSchedule(def,plan,slot),routeTitle=def.displayDistrict||def.district||'Рейс';
    const stopRows=ordered.map((o,i)=>{const sch=adjusted.stops.find(x=>x.orderId===o.id),m=cargoMetricsFromItems(o.items),goods=asArray(o.items).map(x=>`${escapeHtml(x.name)} × ${Number(x.qty||0).toLocaleString('ru-RU')} ${escapeHtml(x.unit||'шт')}`).join('<br>');return`<tr><td class="route-no">${i+1}</td><td class="route-eta"><div class="route-time-main">${escapeHtml(sch?.eta||'—')}</div></td><td class="route-contact"><b>${escapeHtml(o.number)}</b><br>${escapeHtml(o.contactName||'—')}<br><b>${escapeHtml(o.contactMethod||'—')}</b></td><td class="route-address"><b>${escapeHtml(o.deliveryAddress)}</b>${o.driverNote?`<div class="print-stop-note">${escapeHtml(o.driverNote)}</div>`:''}</td><td class="route-goods">${goods||'—'}<div class="print-stop-note">${formatVolume(m.volumeM3)} · ${formatWeight(m.weightKg)}</div></td><td class="route-leg">${sch?.distance?formatKm(sch.distance):'—'}<br>${sch?.travelMin?`${sch.travelMin} мин`:'—'}</td><td class="route-mark"><span class="print-check"></span>Доставлено<br><span class="print-check"></span>Частично<br><span class="print-check"></span>Не доставлено<br>Факт: ______</td></tr>`}).join('');
    const depotReturn=returns?`<tr class="print-depot-row"><td class="route-no">С</td><td class="route-eta"><div class="route-time-main">${escapeHtml(adjusted.finish||'—')}</div></td><td colspan="3"><b>Возврат на склад</b><br>${escapeHtml(settings.warehouse.address)}</td><td class="route-leg">—</td><td class="route-mark">Факт: ______</td></tr>`:'';
    $('printArea').innerHTML=`<div class="print-sheet print-route-sheet print-route-sheet-compact"><div class="print-header"><div class="print-brand"><div class="print-brand-mark" role="img" aria-label="Теплица78"></div><div><div class="print-brand-name">ТЕПЛИЦА78 · ЗАКАЗЫ И ЛОГИСТИКА</div><div class="print-brand-sub">Маршрутный лист водителя</div></div></div><div class="print-doc-box"><div class="print-doc-type">Маршрутный лист</div><div class="print-doc-number">${escapeHtml(routeTitle)}</div><div class="print-doc-status">${returns?'Рейс в круг':'Без возврата'}</div></div></div><div class="print-meta-row"><span>Дата рейса: <b>${formatDateOnly(def.date)}</b></span><span>Схема: <b>${escapeHtml(routeModeText())}</b></span><span>Распечатан: <b>${printedAt}</b></span></div><div class="print-section"><div class="print-section-title">Водитель и автомобиль</div><div class="print-grid"><div class="print-cell"><span>Водитель</span><b>${driver?escapeHtml(driver.name):'Не назначен'}</b></div><div class="print-cell"><span>Телефон</span><b>${driver?escapeHtml(driver.phone||'—'):'—'}</b></div><div class="print-cell"><span>Автомобиль</span><b>${driver?escapeHtml(`${driver.brand} ${driver.model}`):'—'}</b></div><div class="print-cell"><span>Государственный номер</span><b>${driver?escapeHtml(driver.plate||'—'):'—'}</b></div><div class="print-cell print-span-2"><span>Кузов</span><b>${driver?escapeHtml(`${driver.bodyLength} × ${driver.bodyWidth} × ${driver.bodyHeight} м · ${formatVolume(driverBodyVolume(driver))}`):'—'}</b></div><div class="print-cell"><span>Грузоподъёмность</span><b>${driver?formatWeight(driver.payloadKg):'—'}</b></div>${slot?`<div class="print-cell"><span>Плановый выезд</span><b>${escapeHtml(slot.departureTime||'—')}</b></div>`:''}</div></div><div class="print-section"><div class="print-section-title">Параметры рейса</div><div class="print-info-strip"><div class="print-cargo-box"><span>Точек</span><b>${ordered.length}</b></div><div class="print-cargo-box"><span>${returns?'Пробег в круг':'Пробег'}</span><b>${formatKm(plan.distance)}</b></div><div class="print-cargo-box"><span>Движение</span><b>${formatDuration(plan.duration/60)}</b></div><div class="print-cargo-box"><span>Разгрузка</span><b>${formatDuration(rules.unloadMin)}</b></div><div class="print-cargo-box"><span>Полное время</span><b>${formatDuration(rules.totalMin)}</b></div></div><div class="print-cell print-address-cell"><span>Склад</span><b>${escapeHtml(settings.warehouse.address)}</b></div></div><div class="print-payment-total-only"><span>Итого к начислению</span><b>${money(Number(paymentInfo.total||0))}</b></div><div class="print-section print-route-stops-section"><div class="print-section-title">Последовательность точек доставки</div><table class="print-table print-route-table"><colgroup><col style="width:4%"><col style="width:8%"><col style="width:19%"><col style="width:29%"><col style="width:20%"><col style="width:8%"><col style="width:12%"></colgroup><thead><tr><th class="num">№</th><th class="num">Прибытие</th><th>Заказ / контакт</th><th>Адрес / примечание</th><th>Товар / груз</th><th class="num">Переезд</th><th>Отметка водителя</th></tr></thead><tbody><tr class="print-depot-row"><td class="route-no">С</td><td class="route-eta"><div class="route-time-main">${escapeHtml(adjusted.startTime||settings.routeStartTime)}</div></td><td colspan="3"><b>Выезд со склада</b><br>${escapeHtml(settings.warehouse.address)}</td><td class="route-leg">—</td><td class="route-mark">Факт: ______</td></tr>${stopRows}${depotReturn}</tbody></table></div><div class="print-operations"><div class="print-operation"><span>Одометр при выезде</span><b></b></div><div class="print-operation"><span>${returns?'Одометр при возврате':'Одометр на последней точке'}</span><b></b></div><div class="print-operation"><span>Фактический выезд</span><b></b></div><div class="print-operation"><span>${returns?'Фактический возврат':'Фактическое завершение'}</span><b></b></div></div><div class="print-sign"><div class="print-sign-box"><div class="print-sign-line">Диспетчер / логист</div></div><div class="print-sign-box"><div class="print-sign-line">Склад</div></div><div class="print-sign-box"><div class="print-sign-line">Водитель</div></div></div></div>`;
    doPrint();
  };

  cleanupRouteUiV591();
})();
